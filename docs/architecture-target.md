# Целевая архитектура proto-moon

Документ отвечает на один вопрос: **как этот репозиторий должен быть устроен**, чтобы он
выдержал три вещи, ради которых существует, — жить рядом с корпоративным ДС, который мы
не контролируем; принимать правки от людей, которых мы не контролируем; быть
скопированным в проект другого бизнес-юнита.

**Как здесь принимаются решения.** Правильного и неправильного варианта нет — есть идеи,
у каждой три измерения:

- **Сложность** — что надо построить и понять, сколько узлов затрагивается, где
  неочевидные места. Не в часах и не в строках кода, если они не замерены.
- **Реализуемость** — можно ли сделать здесь и сейчас: статика без сборки, Node 20+, ESM,
  без новых зависимостей, закрытый апстрим, чужие права и системы. Где упирается в чужое
  решение — названо, чьё.
- **Масштабируемость** — держится ли идея, когда прототипов станет больше, подключится
  другой бизнес-юнит, править начнут люди вне команды, обновится ДС.

Из этого следует то, что раньше формулировалось как «критерий правильности»: **стоимость
миграции и риск поломки — не причина не предлагать.** Конфликты в открытых ветках,
поломка публичных ссылок, привычки команды — свойства *перехода*; они определяют порядок и
способ работ (раздел 4), а не то, какие идеи вообще рассматриваются. Идея, которая
сломает ссылки, не отбрасывается — рядом с ней планируется починка.

Отсутствие приговора не означает отсутствия выбора: **у каждой идеи со внятной
альтернативой сказано, какую рекомендую и почему.** Альтернатива видна, чтобы решение
можно было оспорить, а не чтобы уклониться от него.

**Как читать.** Раздел 1 — целевое состояние и инварианты: это свойства, которых мы хотим
достичь, а не способ. Раздел 2 — чем сегодняшний репозиторий от них отличается, с
замерами. Раздел 3 — идеи: как этих свойств можно достичь, каждая с артефактом, тремя
осями и рекомендацией. Раздел 4 — порядок перехода. Раздел 5 — вопросы. Раздел 6 — как
проверялось.

Все числа замерены на коммите `5436c38`. **Метод замера указан рядом с каждым числом,
которое зависит от метода** — это не педантизм: два разумных способа посчитать «внешние
зависимости» отличаются вчетверо, и без указания метода число нельзя воспроизвести.
Сводка — приложение Б.

---

## 1. Целевое состояние

### 1.1. Раскладка

Три корзины, разделённые **по происхождению и праву на правку**, а не по «примитивности»
компонента:

```
ds/                      ← апстрим корпоративного ДС. Не правим — вообще, включая
  tokens.css                форматирование. Обновляется только переносом сверху.
  animations.css
  fonts.css  fonts/      ← OK Sans + Roboto (нужен для .__font-roboto)
  components/            ← только файлы с DS-Origin: upstream
  assets/icons/
  index.css              ← точка входа ДС: импортирует ТОЛЬКО ds/
  preview.html           ← витрина чистого ДС

ds-local/                ← наши расширения ДС. Правим. Кандидаты «наверх», в апстрим.
  components/            ← карта __slot-*, дописки к button, button-circle, tag,
                            promo-banner, type-scale-ext, поведенческий JS
  index.css              ← @import '../ds/index.css' + свои

app/                     ← приложение. Пишется заново под каждый бизнес-юнит.
  app.css                ← @import '../ds-local/index.css' + app/components
  components/            ← продуктовое: feed-*, vvz-*, clip-*, gift-card, nv-*, koleso-*
  *.html                 ← экраны
  activity-lenta/  events-lenta/  new-vision/  koleso/
  data/                  ← запечённый контент + схемы карточек
  assets/                ← всё, кроме icons
  preview-app.html       ← витрина продуктовых компонентов

scripts/  docs/  .githooks/  .github/  server.mjs   ← инфраструктура и правила
```

**Приёмочный тест границы**, он же критерий, по которому реорганизация считается
удавшейся: скопировать `ds/` и `ds-local/` в пустой репозиторий, открыть
`ds/preview.html` — витрина **открывается и полностью отрисовывается** без единой правки
пути. «Открывается» недостаточно: сегодняшняя витрина содержит в собственной разметке
продуктовые компоненты (`feed-memory` на строке 701, `feed-base` 728 и 891,
`feed-discussion` 759, `feed-questions` 793 — проверено), поэтому после разделения точек
входа она открылась бы с неотрисованной половиной и тест бы врал. Отсюда требование к
Р7: витрина расщепляется на `ds/preview.html` и `app/preview-app.html`.

Сегодня этот тест невозможен, и **именно эта невозможность — единственная причина
реорганизации.** Не чистота корня.

### 1.2. Инварианты

Двенадцать свойств. Каждое либо выполняется, либо является долгом с датой и владельцем.
Формулировка утвердительная и проверяемая.

**Инвариант — это свойство, а не способ.** «Чем удерживается» в последней колонке —
ссылка на *идею* из раздела 3, то есть на текущий рекомендованный способ достичь этого
свойства. Способ можно заменить другим, если он лучше по трём осям; свойство при этом
остаётся. Именно поэтому инварианты не обсуждаются в терминах «дорого» — они не про
работу, а про то, каким должен быть результат.

| # | Инвариант | Зачем | Чем удерживается |
|---|---|---|---|
| **I1** | У каждого файла ДС объявлено происхождение: `DS-Origin`, `DS-Source`, `DS-Snapshot`, `DS-Local-Patch`. Значение `unknown` допустимо; **отсутствие поля — нет** | Без этого нельзя ответить «что можно править» и «что копировать в другой БЮ». Это первый вопрос при любом переезде и при любом апдейте | Р2, Р3, линтер R12 |
| **I2** | Апстримный файл не правится **вообще**, включая форматирование и разрезание на части. Наше расширение — отдельный файл в `ds-local/`. Дифф файла `ds/` против его снапшота пуст | Любая правка внутри апстримного файла делает будущий апдейт конфликтом. С `button.css` и `icon.css` это уже случилось | Р2 (поле `DS-Local-Patch`), Р11, линтер R13 |
| **I3** | Точка входа ДС не знает о приложении. `ds/index.css` импортирует только `ds/`. Копирование `ds/` + `ds-local/` в другой проект — копирование папок, ноль правок | Иначе «взять ДС» означает «вычистить чужой продукт из точки входа» — ручная работа при каждом копировании | Р7, линтер R14, приёмочный тест 1.1 |
| **I4** | Публичный адрес прототипа — контракт. Путь в файловой системе — не контракт. Адрес переживает перемещение файлов на **всех** каналах публикации | Пока адрес == путь, любое улучшение структуры оплачивается поломкой чужих ссылок, и архитектура становится заложником файловой раскладки | Р8, `docs/urls.json` как источник истины |
| **I5** | Вариант прототипа **наследует** родителя, а не копирует его. У каждого куска байтов ровно один владелец: **родитель** (структура вне генераторных регионов), **генератор** (всё между маркерами), **ребёнок** (дельта). Граница «своё / унаследованное» проходит физически по папке ребёнка: чего в ней нет — унаследовано | Копия означает, что каждое изменение ДС применяется N раз вручную и расходится. Уже разошлось — и замер показал, что расходится в основном **не разметка**, а данные и CSS/JS, которым некуда лечь | Р17, линтер R17-R21 |
| **I13** | Каждое отступление от веб-ДС объявлено машинно-читаемо **в момент, когда его делают**, а не восстанавливается потом. Файл объявляет, какие классы он даёт (`DS-Provides`) и чем его контракт расходится с апстримным (`DS-Deviation`) | Фронтендер держит в руках **экран**, а не файл. Без объявленного владельца класса резолвер врёт: на `messages.html` греп разрешает `.button-container` в `actions-bar.css:75` вместо `button.css:88`, а `.tabbar` — в `nv-tabbar.css:39` вместо `tabbar.css`. Плюс 54 файла из 82 пишут в шапке «OK Design System», будучи продуктовыми | Р2 (поля), Р18 (производные), линтер R22-R26 |
| **I6** | Сгенерированное отделено от написанного и воспроизводимо без сети. Всё между `FEED:START`/`FEED:END` — только машинное. Каждый генератор поддерживает `--offline`, `--check` и идемпотентен | Пока реген требует сети и живой таблицы, «перегенерируется» ≠ «восстановимо», и любая структурная правка упирается в доступ к чужому сервису | Р10, линтер R11 |
| **I7** | Рантайм прототипа не зависит от чужих сетей: ни шрифтов, ни библиотек, ни изображений — включая те, что подставляются скриптом в рантайме | Прототип должен открываться через год и без интернета. Плюс чужие лица на нашем публичном адресе — отдельный риск | Линтер R15, `docs/structure.md` правило 12 |
| **I8** | Прототип не индексируется ни на одном канале публикации | В репозитории 38 реальных имён и 38 фотографий живых людей, а публикуется он на трёх каналах | Р1, линтер R16 |
| **I9** | `main` достижим только через PR, прошедший проверки. Проверки живут на стороне репозитория, а не в окружении автора правки. **Исключений нет — ни для владельца, ни для хотфикса** | Правило, которое можно обойти сменой редактора или галкой «я админ», — не правило. Хотфикс через PR с нулём аппрувов стоит полторы минуты; это не цена, ради которой стоит держать дыру | Р5, Р6, Р9 |
| **I10** | Конфигурация не объявляет возможностей, которых нет в коде. Процесс, раздающий статику, не имеет права записи в источник истины | Сегодня `docker-compose.yml` раздаёт `GITHUB_TOKEN` и ключи бакета под функциональность, которой в `server.mjs` нет. Это одновременно ложь конфигурации и лишние привилегии | Р4 |
| **I11** | Правило либо действует для всего репозитория, либо его нет. Легаси не смягчает правило, а попадает в **baseline**, который может только уменьшаться, причём **пофайлово**: тронутый файл не имеет права стать хуже | Правило с исключениями «потому что так исторически» перестаёт быть правилом через один спор. Пофайловая гранулярность превращает «когда-нибудь почистим» в «сегодня, в этом файле» | `docs/baseline.json` (Р5) |
| **I12** | Контент из внешнего источника проходит те же ворота, что и код: у листа есть схема, генератор валидирует и падает на нарушении, результат приезжает в `main` через PR | Основной рабочий путь дизайнера — таблица → генератор → запечённый HTML. Сегодня он целиком вне архитектуры: схемы есть, но **ни один генератор их не читает** (проверено), валидации нет, владельца нет | Р10, Р16 |

Отдельно — **что инвариантами не является**, чтобы это не путали с архитектурой: число
страниц в корне, красота имён папок, количество прототипов. Это следствия, а не цели.

---

## 2. Разрыв

### 2.1. Инвариант против сегодняшнего состояния

| Инвариант | Сегодня | Замер |
|---|---|---|
| **I1** | **Нарушен.** Заголовок `Source:` есть у **11 из 82** CSS в `components/`: 8 апстримных (`button`, `button-inline`, `content`, `contents-view`, `icon`, `tooltip`, `uni-card`, `uni-cell`) и 3 «сверстано по Figma» (`button-circle`, `tag`, `promo-banner`). У **71** файла — ничего. Восстановить из истории нельзя: клон shallow | `grep -l 'Source:' components/*.css` |
| **I2** | **Нарушен, и это уже стоило нам обновляемости.** `components/button.css` несёт `Source: …/Button.styl` и содержит наши дописки на строках 167-175 (инверсия глифа в тёмной теме), 177-193 (`__style-positive/negative/destructive`), 206-208 (`__style-ai-gift` с захардкоженным градиентом). `components/icon.css` — апстримная база плюс **локально изобретённая** карта 30 слотов `__slot-*` (строки 79-108), которой в апстриме нет вовсе | чтение файлов |
| **I3** | **Нарушен глубоко.** `index.css` — 73 `@import`, из них **68 компонентов**; апстримных среди них только **7** (`tooltip.css` в точку входа даже не включён), остальные **61** — наши. То есть «точка входа ДС» на 90% состоит из не-ДС. Кроме того `tokens.css:78-122` приклеивает типографические классы `ds-*` к селекторам компонентов — `.header.__size-xl .header__title`, `.button-wrapper.__size-56/44/36 .button-container`, `.nav-bar__title`, `.button-inline.__size-16/20/24/32`, `.text-input.__size-36/44/56`, и на строке 95 — **к локальному продуктовому классу `.ll-publish-btn`**. Файл токенов знает не только о компонентах ДС, но и о нашем продукте | `grep -c '@import' index.css`, чтение `tokens.css` |
| **I4** | **Отсутствует как понятие.** Красивые адреса (`/q3`, `/activity`, `/nv`, `/events`, `/preview`) существуют только в `server.mjs:27-33`, то есть только на VM. GitHub Pages (`deploy-pages.yml`, `path: '.'`) и SourceCraft Sites (`.sourcecraft/sites.yaml`, `ref: main`) отдают корень как есть: там адрес == путь | чтение конфигов |
| **I5** | **Нарушен, но не там, где казалось.** Расхождение `events-lenta/` с `activity-lenta/` разрезано по зонам (см. 2.4): из 1552 изменённых строк **1128 (73%) — генераторные регионы**, 191 — инлайновый CSS, 96 — инлайновый JS, и только **111 — собственно разметка**. `view.html` и `add-friends-sheet.html` расходятся **только параметрами**: имя папки, `<title>`, ключ `sessionStorage` | `diff a b \| grep -cE '^[<>]'` по зонам |
| **I13** | **Нарушен, и хуже, чем просто «не объявлено» — конфигурация активно дезинформирует.** 54 файла из 82 в `components/` пишут второй строкой `OK Design System — <Name>`, включая заведомо продуктовые (`FeedBirthday`, `MeshokUp`, `App Shell`, `FeedQuestions`); суффикс `(proto)` есть у 16 и проставлен непоследовательно (`vvz-card` помечен, `meshok-up` — нет); 12 файлов без шапки вовсе. Поля `DS-Provides` нет ни у одного файла | `grep -l '^ \* OK Design System'` |
| **I6** | **Нарушен наполовину.** `--offline` есть у **4 генераторов из 9** (`fetch-feed`, `fetch-marathon`, `fetch-profile`, `fetch-q3`); остальным нужен доступ к Google-таблице. `sheet-cache` пропустит шаг без `--force`; `fetch-all.mjs` не стартует без `sharp`. При этом 692 из 1512 ссылок на `assets/` в HTML лежат внутри `FEED`-блоков | `grep -ln offline scripts/fetch-*.mjs` |
| **I7** | **Нарушен.** По атрибутам `src=`/`href=` в HTML — **250 вхождений, 15 разных хостов**: `fonts.googleapis.com` 76, `i.pravatar.cc` 67, `fonts.gstatic.com` 38, `i.okcdn.ru` 19, `picsum.photos` 15, `upload.wikimedia.org` 12, `vki9.okcdn.ru` 11, `cdn.jsdelivr.net` 4, `i.pinimg.com` 2, плюс по одному `super.ru`, `s0.rbk.ru`, `otkritkiok.ru`, `ok.ru`, `media.tenor.com`, `images.unsplash.com`. **Плюс то, что этот замер не видит:** `lottie-web` подключается на 11 страницах, но атрибутом `src=` — только на 4; остальные 7 инжектят его скриптом (`lenta-q3.html:2577` — `s.src = 'https://cdn.jsdelivr.net/…'`) | `git grep -oE '(src\|href)="https?://[a-z0-9.-]+' -- '*.html'` |
| **I8** | **Нарушен на двух каналах из трёх — P0.** `X-Robots-Tag: noindex` стоит в `server.mjs:100`, роут `/robots.txt` — в `server.mjs:103`. Файла `robots.txt` в репозитории **нет**; `<meta name="robots">` в HTML — **0 вхождений**. На Pages и SourceCraft Sites прототип отдаётся без ограничений, а в нём `data/people.json` с **38** реальными именами и `assets/people/` — **38** фотографий, 4 МБ | `ls robots.txt`, `git grep -i noindex -- '*.html'` |
| **I9** | **Нарушен — P0.** Workflow с триггером `pull_request` — **ни одного**; оба существующих на `on: push`. Единственная автопроверка — `.githooks/pre-commit`, включаемый SessionStart-хуком `.claude/settings.json`, то есть только у владельца в Claude Code | чтение `.github/workflows/*` |
| **I10** | **Нарушен — P0, и шире, чем один файл.** `docker-compose.yml:15-31` раздаёт `SYNC_ON_START`, `GITHUB_TOKEN`, `SYNC_GIT_PUSH_URL`, `SYNC_GIT_BRANCH=main`, `SYNC_GIT_COMMIT`, весь блок `UPLOADS_*` и утверждает, что «`server.mjs`/пайплайн пишут и отдают медиа из бакета». `Dockerfile:3-7` утверждает, что «внутри крутится `server.mjs`: раздаёт статику **и сам пересобирает ленту из Google-таблицы**», а строки 14-17 ставят в образ `git` «для коммита/пуша синка». В `server.mjs` (163 строки) нет ни роута `/content`, ни `upload`, ни S3, ни синка, ни git — все совпадения оказались `Content-Type` и `viewport … content=`. `scripts/lib/media-cache.mjs`, которому `UPLOADS.md` приписывает «dual-mode» с бакетом, не содержит **ни одного** упоминания бакета или `UPLOADS_*` | чтение `server.mjs`, `Dockerfile`, `docker-compose.yml`, `media-cache.mjs` |
| **I11** | **Отсутствует как механизм.** По правилу R9 целиком (`#hex`/`rgba(`/`hsla(` по `html/css/js/mjs`) — **1344** вхождения. Инлайн-`style=` — 616, `<style>`-блоки на 45 страницах из 46. Правила `CLAUDE.md §0` не выполняются массово, и никакого учёта долга нет | `git grep -oE '#[0-9a-fA-F]{3,8}\b\|rgba?\(\|hsla?\('` |
| **I12** | **Нарушен полностью.** Схемы карточек существуют — `data/q3-feed.schema.json` (27 КБ) и `data/feed.schema.json` (13 КБ), — но **ни один скрипт в `scripts/` не содержит слова `schema`**: генераторы их не читают. Валидации листа нет; есть единственная точечная проверка `verify-activity-text.mjs` на один файл данных. Контракт карточек — документация, а не ворота | `git grep -ln schema -- 'scripts/*.mjs'` → пусто |

### 2.2. Три находки, требующие действия немедленно

Это не «первый этап плана». Это то, что вредит, пока документ читают.

**P0-1. Прототип с персональными данными публикуется без запрета индексации.**
`noindex` есть только на VM-канале (`server.mjs:100`). На Pages и SourceCraft Sites нет
ни заголовка, ни `robots.txt`, ни `<meta>`. Публикуются 38 реальных имён и 38 фотографий.
Каждый push в `main` переопубликовывает это заново. Решение — Р1.

**P0-2. Прямой push в `main` уезжает в прод на три канала без единой проверки.**
Ни одного `pull_request`-workflow; локальный хук включается только у владельца. Решение —
Р5, Р6, Р9.

**P0-3. Конфигурация раздаёт секреты под несуществующую функциональность.**
`GITHUB_TOKEN` с правом пуша в `main` и ключи S3 прокидываются в контейнер ради кода,
которого нет; в образ ставится `git` ради той же несуществующей функции. Решение — Р4.

### 2.3. Baseline: долг, а не исключения

Инвариант I11 запрещает смягчать правило ради легаси. Вместо этого нарушения фиксируются
пофайлово и **могут только уменьшаться**. Это единственный честный способ ввести правило
в живой репозиторий: правило действует с первого дня для всего нового, старое видно,
посчитано, имеет владельца и дату.

| ID | Долг | Объём (метод замера) | Против чего | Волна |
|---|---|---|---|---|
| D-1 | Файлы ДС без объявленного происхождения | 110 из 121 (`grep -L 'Source:'`) | I1 | 2 |
| D-2 | Дописки внутри апстримных файлов | `button.css` (3 блока: 167-175, 177-193, 206-208), `icon.css` (карта 30 слотов, 79-108) | I2 | 5 |
| D-3 | Не-ДС в точке входа ДС | 61 из 68 импортов компонентов в `index.css` | I3 | 3 |
| D-4 | `tokens.css` знает о компонентах и о продукте | строки 78-122, включая `.ll-publish-btn` (95) | I3, I2 | 3 |
| D-5 | Прототип-копия вместо наследника | `events-lenta/`: 1552 изменённые строки, из них разметки 111 | I5 | 4 (Р17) |
| D-6 | Генераторы без `--offline` | 5 из 9 | I6 | 5 |
| D-7 | Внешние сети в рантайме | 250 вхождений в атрибутах, 15 хостов; плюс 7 страниц с инжектом скриптом | I7 | 5 |
| D-8 | Литералы дизайн-значений | 1344 по `html/css/js/mjs`; из них **138 в `components/*.css`** — отдельная позиция, см. ниже | I11 | 1 (ratchet) + 5 (ДС) |
| D-9 | Страницы без общей точки входа стилей | 6 из 46: `new-vision.html`, `start-lenta.html`, `q3-view.html`, `activity-lenta/view.html`, `events-lenta/view.html` (подключают отдельные `components/*.css`) и `index.html` (мета-редирект без стилей) | I3 | 3 |
| D-10 | Прототипы без объявленного режима изоляции | `new-vision/` и `koleso/` — 0 тегов `<base href>`, 0 подключений `proto-contain.js` | I5 | 3 (объявить), 4 (в манифест как `kind`) |
| D-11 | Мусор в дереве | папка `assets ` (5 файлов, 500 КБ), `feed-content.json`, `comment-as-feed-twitter.html`, `.deploy-trigger`, `.fig2.png`, `assets/post_crop_small_1080 (2) 1.png` — у всех 0 входящих ссылок | гигиена | 0 |
| D-12 | Документация, расходящаяся с кодом | `README.md:10-35` (4 несуществующих файла), `UPLOADS.md` (несуществующая система целиком), `Dockerfile:3-7`, комментарий `docker-compose.yml` | I10 | 0 |
| D-13 | 209 открытых веток, содержимое 188 непроверяемо | merge-base с `main` доступен у 21 из 210 (клон shallow) | гигиена | 4 (предусловие) |
| D-14 | Сопровождение правил объявлено, но не заведено | «раз в неделю по крону» в `CLAUDE.md:105`, `macket-insights-curator.md:3`, `kirill.md:927`; планировщика нет | I9 | 1 |
| D-15 | Контент не валидируется | 2 схемы существуют, 0 генераторов их читают | I12 | 2 |
| D-16 | Общий слой знает имена детей | `screen-transition.js` — 15 упоминаний `new-vision` (оверлей-механика на строках ~30-228 из 291); `tab-bar.js:9-24` — блок под `events-lenta`; `fetch-q3.mjs` — **15** вхождений `IS_EVENTS`; плюс по одному упоминанию в `activity-indicator.js`, `activity-widget.js`, `invite-list.js`, `moment.js`, `ok-tabbar.js`, `people-data.js` | I5 | 4 (Р17) |
| D-17 | Владелец класса не объявлен | `DS-Provides` нет ни у одного из 121 файла | I13 | 2 (Р18) |
| D-18 | Классы, не разрешающиеся никуда | из классов в разметке 369 разрешаются в `components/`, 377 определены только в `<style>` своей страницы, **159 не определены нигде**; уникальных `ll-*` — 94, в `components/` определено 9 | I13 | baseline (R25) |
| D-19 | Шапки файлов дезинформируют | 54 файла заявляют «OK Design System», будучи в основном продуктовыми | I13 | 2 (Р18) |

**D-8 — два разных долга, и смешивать их нельзя.**
*138 литералов в `components/*.css`* — это ДС. У каждого значения есть токен в
`tokens.css`, замена механическая, результат проверяется витриной. Это **одна работа с
владельцем и датой**, а не фон: волна 5, целевое состояние — ноль.
*Остальные ~1200 в HTML и инлайн-стилях* — продуктовая разметка, которую всё равно
перепишут. Для них назначается **ratchet, а не сроки**: пофайловый baseline (I11) не даёт
тронутому файлу стать хуже, а любая правка внутри файла обязана уменьшить его счётчик
или оставить прежним. Формулировка «почистим по мере касания» без механизма означала
нулевую скорость; с пофайловым baseline скорость равна скорости правок, и это назначено,
а не обещано.

Формат хранения — `docs/baseline.json` (Р5).

### 2.4. Диагноз по форкам: расходится не то, что казалось

D-5 переставлен из «отдельной задачи без решения» в волну 4, и вот почему. Расхождение
`activity-lenta` / `events-lenta` разрезано по зонам владения — дифф разбит по маркерам
генератора и границам инлайновых `<style>`/`<script>`, каждая зона продиффена отдельно
(`diff | grep -cE '^[<>]'`):

| страница | генератор | инлайн CSS | инлайн JS | разметка | всего |
|---|---:|---:|---:|---:|---:|
| `lenta.html` | 774 (79%) | 91 | 72 | **41** | 974 |
| `okruzhenie.html` | 354 | 100 | 24 | 70 | 544 |
| `view.html` | — | — | — | **0** | 14 |
| `add-friends-sheet.html` | — | — | — | **0** | 20 |
| **итого** | **1128 (73%)** | **191** | **96** | **111** | **1552** |

*Оговорка к таблице: суммы по зонам расходятся с общим числом строк на 4 в каждой из двух
больших страниц — границы зон пересекаются на строках, которые одновременно закрывают блок
и несут разметку. На порядок величин (73% расхождения — это данные) это не влияет.*

**Что из этого следует.** Проблема «HTML не наследуется» — это **111 строк разметки**, а
не 974. Остальное — данные, которые генератор уже умеет разводить, и CSS/JS, оказавшиеся
внутри HTML только потому, что **ребёнку некуда положить свой CSS и свой JS**. Две малые
страницы расходятся исключительно параметрами. Это и есть основание для Р17: задача не
«свести два разошедшихся форка», а «дать ребёнку три места, куда класть дельту».

**Форк делали `sed`-ом, и след остался.** Слепая замена `_al` → `_ev` переименовала
CSS-класс `friend-big-card__all` → `friend-big-card__evl` — 6 вхождений в
`events-lenta/add-friends-sheet.html` (проверено). Визуально цело только потому, что класс
определён в инлайновом `<style>` той же страницы и переименовался согласованно. Отсюда
жёсткое требование к подстановке параметров в Р17: она обязана быть **позиционной**, иначе
следующий такой форк сломает разметку молча.

**Контраст с «хорошим» наследником.** `new-vision/lenta.html` — один `<link
rel=stylesheet>` и 14 строк инлайнового CSS. `activity-lenta/lenta.html` — 8 линков и 415
строк инлайна, `events-lenta/lenta.html` — 8 и 489. Разница не в аккуратности авторов, а в
том, что у NV есть куда положить своё, а у `events` — нет.

**Цена копии видна и в истории.** Коммит создания `events-lenta` (`b85ff16`) добавил 6843
строки, из которых дельта — меньше десятой части, и **правил пять общих файлов**:
`index.css`, `components/icon.css`, `header.css`, `tabs.css`, завёл `divider.css`. В том
числе `.tabs.__size-l { font-size: 21px; line-height: 28px; }` — хардкод типографики в
общий слой ради одного ребёнка.



---

## 3. Идеи

Каждая идея подана одинаково: **что делаем** (готовый артефакт), **какая альтернатива
рассматривалась**, **три оси** и **что рекомендую**. Где альтернативы по существу нет —
это сказано прямо, и оси даны одной строкой; выдумывать соломенное чучело ради симметрии
таблицы я не стал.

Где артефакт нельзя дописать без чужого решения, стоит явная дырка со ссылкой на вопрос
из раздела 5. Тип внешнего источника: **[док]** — документация вендора, **[инж]** —
инженерный блог, **[форум]** — живой опыт. ✅ — страницу открывал лично; ○ — из выдачи
поиска, страницу не открывал.

**Сводка выбора** — чтобы не читать весь раздел ради ответа «что предлагается»:

| Идея | Что рекомендую | Главный довод |
|---|---|---|
| Р1 | `robots.txt` + `<meta>` на всех страницах | единственный механизм, работающий на статических каналах |
| Р2 | шапка в самом файле, а не внешний реестр | провенанс переживает перемещение файла и виден в диффе |
| Р3 | манифест генерируется из шапок | один источник истины, сводка не расходится с файлами |
| Р4 | `UPLOADS.md` удалить, а не пометить | ни одно его утверждение не подтверждается кодом |
| Р5 | пофайловый baseline | единственный из трёх вариантов, который даёт правилу подействовать сегодня и не остановиться |
| Р6 | отчёт в сводку прогона, без комментария в PR | работает для форков без дополнительных прав |
| Р7 | оверрайд в `ds-local/`, не разрезание `tokens.css` | сохраняет право на пустой дифф с апстримом |
| Р8 | `urls.json` + статические заглушки | переезд файлов перестаёт быть событием для внешнего мира |
| Р9 | защита без права обхода | дыра ценна ровно тогда, когда цена ошибки максимальна |
| Р10 | валидация в генераторе | контент проходит те же ворота, что и код |
| Р11 | вопрос агента поверх детерминированного R13 | R13 ловит факт, агент выясняет намерение |
| Р12 | ссылки на Figma-ноды сохранить как исторический след, сверку не строить | Figma уходит; автосинхронизация с ней всё равно упиралась в потолок ~2% |
| Р12а | целевой источник правды — веб-ДС в GitLab; до ответа фактический источник — сам прототип | единственный вариант, масштабируемый на другие БЮ, но он зависит от вопроса 1 |
| Р13 | правила в полной силе + baseline | правило с исключениями перестаёт быть правилом |
| Р14 | `prepare` как удобство, гарантия — в CI | локальный хук в принципе обходится |
| Р15 | удалить и переименовать | у всех 0 входящих ссылок |
| Р16 | напоминание с чек-листом, не запуск агента из CI | исполнимо без ключа и без чужих решений |

---

### Р1. Запрет индексации на всех каналах — I8

**`robots.txt` в корне репозитория** (публикуется как есть и на Pages, и на SourceCraft
Sites):

```
User-agent: *
Disallow: /
```

**В `<head>` каждой страницы** — единственный механизм, работающий на статических каналах
и переживающий перенос файлов:

```html
<meta name="robots" content="noindex, nofollow">
```

Раскладка по 46 страницам — механическая правка, выполняется **в волне 0**, вместе с
`robots.txt`. Это важно для механики baseline: правило R16 приходит в волне 1 уже с
нулём нарушений, и ни один PR между волнами не краснеет. `server.mjs` продолжает отдавать
`X-Robots-Tag` (строка 100) — они не конфликтуют. Партиал
`components/today-widgets.partial.html` — не страница и правилом R16 не проверяется.

**Что это не решает.** Запрет индексации не делает адрес приватным: ссылку по-прежнему
можно переслать. Нужен ли прототипу с ПД настоящий контроль доступа — **вопрос 3**, это
решение владельца данных.

**Три оси.** *Сложность* минимальная: один файл и одна строка в шаблоне страницы.
*Реализуемость* полная, ни от кого не зависит. *Масштабируемость* — держится: правило R16
не даёт новой странице появиться без тега, поэтому проблема не возвращается при росте
числа экранов. Внятной альтернативы нет: заголовок `X-Robots-Tag` доступен только на
VM-канале, а Pages и SourceCraft Sites управлять заголовками не дают.

**Как решают снаружи.** GitHub Pages не даёт управлять заголовками ответа, поэтому на
статическом канале доступны только `robots.txt` в корне публикуемой папки и `<meta>`.
Разбора именно связки «Pages + персональные данные» **не нашёл**.

---

### Р2. Объявленное происхождение файлов ДС — I1, I2

**Шаблон шапки.** Первый блок комментария; формат одинаков для `.css` и `.js`.

```css
/**
 * DS-Origin:      upstream | upstream+local | local | unknown
 * DS-Source:      <путь в апстриме> | —
 * DS-Snapshot:    <ревизия апстрима или дата снятия YYYY-MM-DD> | unknown
 * DS-Provides:    <блочные классы, которые файл объявляет своими>
 * DS-Deviation:   <чем контракт расходится с апстримным — словами, для фронта>
 * DS-Local-Patch: <диапазоны наших дописок и что в них> | —
 * DS-Archive:     <ссылки на выведенные из эксплуатации системы>            (опц.)
 */
```

Шесть полей, три назначения. `DS-Origin`/`DS-Source`/`DS-Snapshot` отвечают на «можно ли
править и с чем сравнивать» (I1, I2). **`DS-Provides` и `DS-Deviation` отвечают на «что
отдавать фронту» (I13) — обоснование и способ применения в Р18.** `DS-Local-Patch`
обслуживает линтер R13. `DS-Archive` — археология, см. Р12.

Значения `DS-Origin` — четыре, а не пять: **`figma` упразднён** (Р12).

**Три заполненных примера из этого репозитория** — по одному на тип.

`ds/components/uni-card.css` — чистый апстрим:

```css
/**
 * DS-Origin:      upstream
 * DS-Source:      design-system/components/UniCard/UniCard.styl
 * DS-Snapshot:    unknown          ← см. вопрос 4
 * DS-Local-Patch: —
 */
```

`ds/components/button.css` — апстрим с необъявленными сегодня дописками. Шапка фиксирует
факт; вынос дописок в `ds-local/` — долг D-2. Поле `DS-Local-Patch` при этом
**функционально**, а не декоративно: по нему правило R13 отличает известную дописку от
новой (см. Р5):

```css
/**
 * DS-Origin:      upstream+local
 * DS-Source:      design-system/components/Button/Button.styl
 * DS-Snapshot:    unknown          ← см. вопрос 4
 * DS-Local-Patch: 167-175 инверсия монохромного глифа в тёмной теме;
 *                 177-193 .__style-positive / .__style-negative / .__style-destructive;
 *                 206-208 .__style-ai-gift (градиент, Figma node 4833:57273);
 *                 ДОЛГ D-2: вынести в ds-local/components/button-ext.css
 */
```

`app/components/feed-header.css` — наше продуктовое (сегодня без шапки вовсе):

```css
/**
 * DS-Origin:      local
 * DS-Source:      —
 * DS-Snapshot:    —
 * DS-Local-Patch: —
 */
```

**Объём:** 121 файл (82 CSS + 35 JS + `tokens.css`, `index.css`, `animations.css`,
`fonts.css`); для 11 данные уже есть в существующих заголовках `Source:`. Ни один путь не
меняется.

**Альтернатива: внешний реестр вместо шапки в файле** — один `docs/ds-origin.json`, где
путь файла сопоставлен происхождению.

| | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| **Шапка в самом файле** | Ниже: правка локальна, никакого связывания по пути. Неочевидное место одно — `tokens.css`, который сам себе запрещает правку, поэтому его запись живёт в манифесте | Полная: комментарий в CSS/JS, ничего не требуется | Держится: файл переезжает вместе со своим происхождением, дифф провенанса виден в том же PR, что и правка кода |
| Внешний реестр | Выше: нужен механизм, который не даёт реестру разойтись с деревом при каждом переименовании | Полная | Хуже: при волне 4 (переезд 121 файла) реестр устаревает целиком; при копировании `ds/` в другой БЮ реестр надо резать вручную |

**Рекомендую шапку в файле.** Решающий довод — волна 4 и копирование в другой БЮ: и то, и
другое перемещает файлы, а внешний реестр при перемещении обязан обновляться отдельным
действием, которое некому проконтролировать. Единственное исключение из этого правила —
`tokens.css`, и оно продиктовано не удобством, а инвариантом I2.

**Про `DS-Provides`.** Поле обязательное, и это не бюрократия: без него резолвер «класс →
файл» врёт — на `messages.html` греп разрешает `.button-container` в `actions-bar.css:75`
вместо `button.css:88`. На этом поле стоят оба производных артефакта передачи — спека
экрана и рантайм-инспектор (Р18).

**Про `DS-Origin: figma`.** Значение упраздняется: указывать на выводимую из эксплуатации
систему как на источник нельзя. Три файла (`button-circle`, `tag`, `promo-banner`)
переходят в `local` — и это буквально верно: аналога в апстриме не знаем, макета скоро не
будет, CSS и есть артефакт. Автоматически становятся кандидатами наверх (Р18). Подробности
и что делать с ключами — Р12.

**Дырки.** `DS-Snapshot` невозможно заполнить, пока неизвестна ревизия слепка и чем он
конвертирован — **вопрос 4**; до ответа во всех апстримных файлах стоит `unknown`, и это
честнее правдоподобной даты. Классификация 71 файла без `Source:` — **вопрос 5**.
Для `tokens.css` шапка неприменима: файл объявляет «Do not edit manually», его
происхождение живёт в манифесте (Р3).

**Как решают снаружи.** ✅ [док] Chromium требует для каждой вендоренной зависимости
`README.chromium` с обязательными полями `Name`, `URL`, `License`, `Shipped`,
`Security Critical`, **`Update Mechanism`** (`Autoroll | Manual | Static |
Static.HardFork`), `Description`, **`Local Modifications`**
([шаблон](https://chromium.googlesource.com/chromium/src/+/HEAD/third_party/README.chromium.template)).
Наши четыре поля — прямая калька. ✅ [инж] Оттуда же правило, забранное дословно в I2: не
переформатировать код внутри зависимости, потому что «сохранение оригинального
форматирования обязательно для получения чистых диффов с апстримом»
([adding_to_third_party](https://chromium.googlesource.com/chromium/src/+/HEAD/docs/adding_to_third_party.md)).
○ [док] Debian держит локальные правки отдельными патчами с DEP-3-заголовками
([gbp.patches](https://honk.sigxcpu.org/projects/git-buildpackage/manual-html/gbp.patches.html)).

**Что нам не подходит.** Патч-подход требует доступной pristine-копии апстрима для
генерации диффа. У нас её нет, а локальная копия ещё и сконвертирована из Stylus вручную.
Поэтому `DS-Local-Patch` — описание диапазонами, а не машинный патч. Ограничение снимается
только ответом на **вопрос 1**.

---

### Р3. `docs/ds-manifest.md` — сводка происхождения — I1

**Как выглядит файл:**

```markdown
# Манифест ДС

Сгенерировано `node scripts/ds-manifest.mjs`. Руками не править — источник истины
шапки DS-Origin в самих файлах.

| Файл | Origin | Source | Snapshot | Наши дописки | В другой БЮ |
|---|---|---|---|---|---|
| `ds/components/uni-card.css` | upstream | `design-system/components/UniCard/UniCard.styl` | unknown | — | да |
| `ds/components/button.css` | upstream+local | `design-system/components/Button/Button.styl` | unknown | 167-175, 177-193, 206-208 | да, с дописками |
| `app/components/feed-header.css` | local | — | — | — | нет |
```

**Генератор целиком** — ESM, Node 20+, без зависимостей. Список каталогов включает и
сегодняшний `components/`, и целевые `ds/`, `ds-local/`, `app/`: манифест работает до
переезда и после, без правок.

```js
#!/usr/bin/env node
/**
 * ds-manifest.mjs — собирает docs/ds-manifest.md из шапок DS-Origin.
 * Запуск: node scripts/ds-manifest.mjs
 */
import { readFileSync, writeFileSync, readdirSync, existsSync } from 'node:fs';
import { dirname, resolve, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '..');
const DIRS = ['components', 'ds/components', 'ds-local/components', 'app/components'];
const EXTRA = ['tokens.css', 'index.css', 'animations.css', 'fonts.css',
               'ds/tokens.css', 'ds/index.css', 'ds/animations.css', 'ds/fonts.css'];

const files = [
  ...DIRS.filter((d) => existsSync(join(ROOT, d)))
         .flatMap((d) => readdirSync(join(ROOT, d))
                          .filter((f) => /\.(css|js)$/.test(f))
                          .map((f) => `${d}/${f}`)),
  ...EXTRA.filter((f) => existsSync(join(ROOT, f))),
].sort();

const field = (src, name) => {
  const m = new RegExp(`^\\s*\\*\\s*${name}:\\s*(.+)$`, 'm').exec(src);
  return m ? m[1].trim() : '';
};
const CARRY = { upstream: 'да', 'upstream+local': 'да, с дописками',
                figma: 'да, с дописками', local: 'нет', unknown: '?' };

const rows = files.map((rel) => {
  const src = readFileSync(join(ROOT, rel), 'utf8').slice(0, 2000);
  const origin = field(src, 'DS-Origin') || 'unknown';
  return `| \`${rel}\` | ${origin} | ${field(src, 'DS-Source') || '—'} `
       + `| ${field(src, 'DS-Snapshot') || '—'} `
       + `| ${field(src, 'DS-Local-Patch') || '—'} | ${CARRY[origin] ?? '?'} |`;
});

writeFileSync(join(ROOT, 'docs/ds-manifest.md'),
  '# Манифест ДС\n\nСгенерировано `node scripts/ds-manifest.mjs`. Руками не править — '
  + 'источник истины\nшапки DS-Origin в самих файлах.\n\n'
  + '| Файл | Origin | Source | Snapshot | Наши дописки | В другой БЮ |\n'
  + '|---|---|---|---|---|---|\n' + rows.join('\n') + '\n');

const unknown = rows.filter((r) => r.includes('| unknown |')).length;
console.log(`ds-manifest: ${rows.length} файлов, без объявленного происхождения — ${unknown}`);
```

**Три оси.** *Сложность* низкая: обход дерева и разбор шапок, никакой логики.
*Реализуемость* полная — ESM, ноль зависимостей. *Масштабируемость* хорошая: список
каталогов включает и сегодняшний `components/`, и целевые `ds/`, `ds-local/`, `app/`,
поэтому генератор переживает волну 4 без правок. Альтернатива — вести сводку руками — не
рассматривается всерьёз: она разойдётся с деревом на первой же неделе.

Счётчик `unknown` — метрика долга D-1, видна одной командой.

**После ухода из Figma манифест меняет назначение** (Р18): из реестра происхождения он
становится **каталогом компонентов** — тем, чем была библиотечная страница. Отсюда две
добавки к таблице, которых нет выше: колонка `Provides` (классы файла) и **ссылка на
живую витрину рядом с каждым компонентом** — каталог без картинки дизайнеру бесполезен.
Второй выход того же генератора — `docs/ds-map.json` (`{"<класс>": {"file":…,"origin":…}}`),
на нём стоит рантайм-инспектор `ds-inspect.js`.

**Как решают снаружи.** ✅ [док/инж] Та же схема у Chromium: метаданные рядом с кодом,
сводка обходом дерева. **Не подходит:** инвентаризация через Storybook — требует сборки и
рантайма, которых в проекте нет.

---

### Р4. Убрать несуществующую функциональность из конфигурации — I10

Три файла, один коммит:

1. **`docker-compose.yml`** — удалить строки 15-31: блок `SYNC_*`, `GITHUB_TOKEN`,
   `UPLOADS_*` и комментарий, утверждающий, что `server.mjs` пишет в бакет.
2. **`Dockerfile`** — переписать шапку (строки 3-7): образ раздаёт статику и ничего не
   пересобирает. Удалить установку `git` (строки 14-17) — она стоит там ради
   несуществующего коммита-синка; `ca-certificates` оставить, если нужны исходящие HTTPS.
3. **`UPLOADS.md`** — **удалить.** Обоснование, а не осторожность: документ описывает не
   отдельную деталь, а систему целиком, которой нет ни в одном месте кода. Роута
   `/content` нет в `server.mjs`; «dual-mode» у `media-cache` нет — в
   `scripts/lib/media-cache.mjs` **ноль** упоминаний бакета и `UPLOADS_*` (проверено).
   Документ, у которого не подтверждается ни одно утверждение, вреднее отсутствия
   документа. Содержимое переезжает в **вопрос 6** как проектное предложение.

Зависимость `@aws-sdk/client-s3` остаётся: её используют три живых разовых скрипта
(`check-bucket.mjs`, `migrate-assets.mjs`, `migrate-clips.mjs`).

**Альтернатива для `UPLOADS.md`: не удалять, а пометить статусом «не реализовано».**

| | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| **Удалить** | Ниже: нечего поддерживать | Полная | Держится: нет документа — нет расхождения с кодом |
| Пометить статусом | Выше: помеченный документ надо сверять с кодом при каждой правке контура, иначе статус протухнет так же, как протухло его содержимое | Полная | Хуже: документ, у которого не подтверждается **ни одно** утверждение, при росте команды читают как правду, а пометку пролистывают |

**Рекомендую удалить.** Довод не в экономии, а в проверке: у `UPLOADS.md` не подтвердилось
ни одно утверждение — ни роут `/content`, ни «dual-mode» у `media-cache` (0 упоминаний
бакета в `scripts/lib/media-cache.mjs`). Пометка статусом уместна для документа,
описывающего существующее с неточностями; здесь описана несуществующая система целиком.
Содержимое не пропадает — оно переезжает в вопрос 6 как проектное предложение.

**Архитектурное условие на будущее.** Если контур загрузки медиа вернут, он не может быть
реализован так, как описан: раздающий процесс не имеет права коммитить в `main` (I10) —
автокоммит с VM обходит PR, защиту ветки и линтер, то есть отменяет I9 целиком. Правильная
форма — сервис создаёт **ветку и PR**. Это условие включения, а не пожелание.

**Как решают снаружи.** Специального разбора кейса «конфиг раздаёт секреты под
несуществующую фичу» **не нашёл**.

---

### Р5. `scripts/lint-macket.mjs` + пофайловый baseline — I9, I11

Детерминированный линтер без зависимостей, по образцу существующего `scripts/nbsp.mjs`.
Все правила жёсткие: I11 запрещает «предупреждения ради легаси».

**Три подхода к легаси — главная развилка всего документа.**

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| A. Включить правила на весь репозиторий сразу | Минимальная в коде | **Отрицательная:** 1344 срабатывания R9 в первый же день; проверку выключат через неделю | Не доживёт до проверки |
| Б. Правила только на добавленные строки диффа | Низкая | Полная | Слабая: файл с 143 литералами так и останется с 143 — правило не создаёт давления на существующий долг, а «почистим по мере касания» без механизма означает нулевую скорость |
| **В. Пофайловый baseline** | Выше обеих: нужны одинаковые единицы измерения в двух режимах, запись «правило × файл», и понимание, что диффовые правила в baseline не попадают | Полная: чистый Node, состояние в одном JSON | Лучшая: правило действует с первого дня для нового; чужой старый долг PR не валит; **тронутый файл обязан не ухудшиться**, а улучшение фиксируется автоматически |

**Рекомендую В.** Довод: только он одновременно даёт правилу подействовать сегодня и не
даёт долгу застыть. Вариант Б выглядит дешевле, но оставляет 1344 нарушения вне всякого
процесса; вариант A честнее по строгости, но не переживёт первой недели. Цена В — та
самая «выше обеих» сложность, и она сосредоточена в одном месте: единица измерения должна
быть одна и та же в обоих режимах, иначе сравнение с baseline бессмысленно.

#### Механика baseline — сначала семантика, потом код

`docs/baseline.json` хранит **число нарушений каждого правила в каждом файле**:

```json
{
  "_": "Пофайловый счётчик нарушений. Заполняется прогоном --baseline. Может только убывать.",
  "generated": "<дата прогона>",
  "counts": {
    "R9": { "lenta-q3.html": 143, "components/button.css": 4 },
    "R15": { "lenta-q3.html": 7 }
  }
}
```

Правила этой механики:

- **Единица измерения одна в обоих режимах** — число совпадений при полном сканировании
  файла. Никогда не «добавленные строки»: их нельзя сравнить с числом в baseline.
- **Отсутствие файла или правила в `counts` означает ноль**, а не «проверку пропустить».
- **PR-режим сканирует изменённые файлы целиком** и сравнивает с их записью в baseline.
  Стало больше — FAIL с указанием, на сколько. Стало меньше — baseline переписывается и
  коммитится вместе с правкой. Ровно столько же — молча проходит.
- Отсюда правило «файл, который трогаешь, выходит не хуже, чем вошёл» — это **и есть**
  механизм, а не лозунг: чужие старые нарушения PR не валят, свои новые — валят.
- **`--baseline` сканирует всё дерево тем же кодом** и теми же правилами. Прогон
  `--baseline` в CI (шаг «Долг не вырос») падает, если суммарно по репозиторию стало
  хуже.

#### Правила и области видимости

| # | Область | Детекция | Сообщение |
|---|---|---|---|
| R1 | `*.html`, `*.css` | Собрать `/(?:src\|href)="([^"]+)"/g` и `/url\(\s*['"]?([^)'"]+)/g`; отбросить `^(https?:)?//`, `#`, `data:`, `mailto:`, `tel:`, `javascript:`. База: файл начинается со строки `^<base href="../">` → резолв от корня репо, иначе от папки файла. Существование — посегментной сверкой с `readdirSync` | `битый путь ассета: «{path}» не существует (или отличается регистром)` |
| R2 | `*.html` | `/<img[^>]+src="[^"]*back_24\.svg"/i` | `кнопка «назад» через <img back_24.svg> — это шеврон «‹». Нужен <span class="icon __size-24 __slot-back">` |
| R3 | `*.html` | Разрешённые размеры вычитать из `avatar.css`: `/^\.avatar\.__size-(\d+)/gm`; в разметке `/class="[^"]*\bavatar\b[^"]*__size-(\d+)/g` | `.avatar.__size-{N} — такого размера нет в avatar.css. Доступны: {список}` |
| R4 | `*.html` | В срезе до `</head>` искать `/<script[^>]*src="[^"]*screen-transition\.js"[^>]*>/`; отдельно наличие `nav-bar__back` | `screen-transition.js обязан быть синхронным <script src> в <head>` |
| R5 | добавленные файлы | Каждый сегмент пути против `/^[a-z0-9]+(?:[-.][a-z0-9]+)*$/` | `имя «{path}» не в kebab-case — docs/structure.md, правило 7` |
| R6 | `*.html` в папках-прототипах | Распарсить `TAB_ALLOW_OUT`/`TAB_ROUTES` из `proto-contain.js`; найти `/class="[^"]*tabbar-icon[^"]*__slot-([a-z]+)/g` | `слот «{slot}» ведёт за пределы {dir}, но его нет в TAB_ALLOW_OUT` |
| R7 | `*.html` с `<base href="../">` | `/(?:src\|href)="\.\.\//` | `на странице с <base href="../"> «../» лишний` |
| R8 | `*.html`, только PR-режим | `spawnSync('node', ['scripts/nbsp.mjs','--check', …html])` | `неразрывные пробелы: node scripts/nbsp.mjs {файлы}` |
| R9 | `*.html`, `*.css`, `*.js`, `*.mjs` | `/#[0-9a-fA-F]{3,8}\b\|rgba?\(\|hsla?\(/` | `литерал цвета — возьми токен из tokens.css` |
| R10 | `*.html`, `*.css` | `/(?:padding\|margin\|gap)\s*:\s*(\d+)px/` со сверкой по `{4:--space-1, 8:--space-2, 12:--space-3, 16:--space-4, 20:--space-5, 24:--space-6}` | `{N}px → var({token})` |
| R11 | `*.html`, только PR-режим | Номер добавленной строки попадает между `FEED:START` и `FEED:END` | `генерируемый блок — правка будет затёрта; меняй лист или шаблон` |
| R12 | файлы из области манифеста (`components/**` + 4 корневых CSS; после переезда — `ds/**`, `ds-local/**`) | Нет `/^\s*\*\s*DS-Origin:/m` | `файл ДС без объявленного происхождения — docs/structure.md, правило 4` |
| R13 | файлы с `DS-Origin: upstream` **или** `upstream+local` | Дифф трогает строку, не входящую ни в один диапазон из `DS-Local-Patch` этого файла | `правка апстримного файла вне объявленных дописок. Расширение кладётся в ds-local/ (I2)` |
| R14 | `ds/index.css` | `@import`, чей путь выходит за `ds/` | `точка входа ДС не может импортировать приложение (I3)` |
| R15 | `*.html`, `*.js` | `/https?:\/\/[a-z0-9.-]+/` — **любой литерал URL, не только в атрибутах** | `внешняя сеть в рантайме — положи локально (I7)` |
| R16 | `*.html`, кроме `*.partial.html` | Нет `<meta name="robots" content="noindex, nofollow">` | `страница без запрета индексации (I8)` |

**Правила R17-R26 живут в своих разделах**, но исполняются тем же скриптом и по той же
механике baseline: **R17-R21** — наследование прототипов (Р17), **R22-R26** — передача
фронтам (Р18). Из них файловые (в baseline): R20, R22, R23, R24, R25. Правила действия
(по диффу, вне baseline): R17, R18, R19, R21, R26.

**Три области видимости названы явно, потому что от этого зависит корректность:**

- **По всему файлу и в baseline:** R1, R2, R3, R4, R6, R7, R9, R10, R12, R14, R15, R16.
  Их можно считать одинаково в обоих режимах, и только поэтому сравнение с baseline имеет
  смысл.
- **Только по диффу, вне baseline:** R5 (имена добавленных файлов), R8 (nbsp), R11
  (правка внутри маркеров), R13 (правка апстримного файла). Это правила про *действие*, а
  не про *состояние*: считать их «по файлу» бессмысленно.
- **`scripts/**` исключён из R1 целиком.** Это не послабление, а корректность: шаблоны
  рендера внутри `fetch-*.mjs` содержат пути, база которых — **целевая страница**, а не
  папка скрипта. `fetch-q3.mjs` пишет `src="assets/icons/…"` (верно для страницы в корне,
  но от `scripts/` резолвится в несуществующий `scripts/assets/…` → ложный FAIL);
  `fetch-feed.mjs:318,370,398,464,465` пишет `../assets/icons/…` и прошёл бы проверку
  случайно. Правило систематически ошибается в обе стороны на файлах пайплайна, поэтому
  его область — только `*.html` и `*.css`. Корректность путей внутри шаблонов
  обеспечивает прогон генератора с `--check` (Р10), а не текстовый линтер.

**Дырка:** R3 сегодня пропустит `.avatar.__size-126` — он определён в `avatar.css:79` с
пометкой `/* legacy */`, но отсутствует и в шапке файла, и в чек-листе агента. **Вопрос
A** (короткий список в разделе 5). До ответа правило читает `avatar.css` как есть.

#### `docs/baseline.json` — стартовое содержимое

Числа **не проставляются вручную**: первый прогон `--baseline` их и запишет. В файл
кладутся только определения — что именно считает правило и по каким файлам, чтобы число
можно было воспроизвести и оспорить:

```json
{
  "_": "Пофайловый счётчик. Заполняется прогоном `node scripts/lint-macket.mjs --baseline`.",
  "generated": null,
  "defs": {
    "R9":  "/#[0-9a-fA-F]{3,8}\\b|rgba?\\(|hsla?\\(/ по *.html, *.css, *.js, *.mjs",
    "R10": "/(?:padding|margin|gap)\\s*:\\s*(\\d+)px/ с токеном-эквивалентом, по *.html, *.css",
    "R12": "нет DS-Origin, по области манифеста",
    "R15": "/https?:\\/\\/[a-z0-9.-]+/ по *.html, *.js",
    "R16": "нет meta robots, по *.html кроме *.partial.html"
  },
  "counts": {}
}
```

**Опорные замеры на дату документа** — чтобы первый прогон было с чем сверить, и чтобы
никто не принял их за содержимое файла:

| Правило | Замер | Метод |
|---|---|---|
| R9 | **1344** | `git grep -oE '#[0-9a-fA-F]{3,8}\b\|rgba?\(\|hsla?\(' -- '*.html' '*.css' '*.js' '*.mjs'` |
| R15 | **250** по атрибутам `src=`/`href=` в HTML; по расширенному правилу (любой литерал URL, включая инжект скриптом) — **больше**, точное число даст первый прогон | `git grep -oE '(src\|href)="https?://' -- '*.html'` |
| R12 | сегодня **110** в области `components/**` + 4 корневых CSS (121 минус 11 с `Source:`) | `grep -L 'Source:'` |
| R16 | **46** страниц; после Р1 (волна 0) — **0** | `git ls-files '*.html'` минус партиал |

#### Каркас — рабочий код

```js
#!/usr/bin/env node
/**
 * lint-macket.mjs — правила сборки макетов с пофайловым baseline.
 *
 *   node scripts/lint-macket.mjs --diff origin/main   — проверка PR
 *   node scripts/lint-macket.mjs --baseline           — пересчёт docs/baseline.json
 *   node scripts/lint-macket.mjs                      — по staged (для pre-commit)
 *
 * Выход: 1 — счётчик вырос хотя бы в одном файле либо сработало диффовое правило.
 */
import { readFileSync, writeFileSync, readdirSync, existsSync } from 'node:fs';
import { execFileSync, spawnSync } from 'node:child_process';
import { dirname, resolve, join, relative } from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '..');
const argv = process.argv.slice(2);
const BASE = argv.includes('--diff') ? argv[argv.indexOf('--diff') + 1] : null;
const REBASE = argv.includes('--baseline');
const BASELINE = join(ROOT, 'docs/baseline.json');
const git = (...a) => execFileSync('git', a, { cwd: ROOT, encoding: 'utf8' });

/** Единственный фильтр расширений — общий для обоих режимов. */
const SCANNABLE = /\.(html|css|js|mjs)$/;

function changedFiles() {
  const out = BASE
    ? git('diff', '--name-only', '--diff-filter=ACMR', `${BASE}...HEAD`)
    : git('diff', '--cached', '--name-only', '--diff-filter=ACMR');
  return out.split('\n').filter(Boolean)
            .filter((f) => SCANNABLE.test(f))
            .filter((f) => existsSync(join(ROOT, f)));
}
const allFiles = () => git('ls-files').split('\n').filter((f) => SCANNABLE.test(f));

/** Добавленные строки — [[номер, текст], …]. Нужны только диффовым правилам. */
function addedLines(file) {
  const range = BASE ? [`${BASE}...HEAD`] : ['--cached'];
  const patch = git('diff', ...range, '--unified=0', '--', file);
  const res = []; let n = 0;
  for (const l of patch.split('\n')) {
    const m = /^@@ -\d+(?:,\d+)? \+(\d+)/.exec(l);
    if (m) { n = Number(m[1]); continue; }
    if (l.startsWith('+') && !l.startsWith('+++')) res.push([n++, l.slice(1)]);
    else if (!l.startsWith('---')) n++;
  }
  return res;
}

/** Существование пути с точной проверкой регистра (Linux case-sensitive). */
function existsExact(abs) {
  const rel = relative(ROOT, abs);
  if (rel.startsWith('..')) return false;
  let cur = ROOT;
  for (const seg of rel.split('/')) {
    let entries; try { entries = readdirSync(cur); } catch { return false; }
    if (!entries.includes(seg)) return false;
    cur = join(cur, seg);
  }
  return true;
}

const isHtml = (f) => f.endsWith('.html');
const isStyleish = (f) => /\.(html|css)$/.test(f);
const inManifestScope = (f) =>
  /^(ds|ds-local)\//.test(f) ||
  /^components\/.*\.(css|js)$/.test(f) ||
  ['tokens.css', 'index.css', 'animations.css', 'fonts.css'].includes(f);

/**
 * ФАЙЛОВЫЕ правила: считаются одинаково в обоих режимах — только поэтому
 * сравнение с baseline осмысленно. Возвращает [{rule, line, msg}, …].
 */
function scanFile(f, src) {
  const out = [];
  const add = (rule, line, msg) => out.push({ rule, line, msg });
  const lineOf = (i) => src.slice(0, i).split('\n').length;
  const baseRoot = /^<base href="\.\.\/">/m.test(src);
  const from = baseRoot ? ROOT : join(ROOT, dirname(f));

  // R1 — только разметка и стили: в scripts/** база путей другая (см. документ).
  if (isStyleish(f) && !f.startsWith('scripts/')) {
    for (const m of src.matchAll(/(?:src|href)="([^"]+)"|url\(\s*['"]?([^)'"]+)/g)) {
      const p = (m[1] ?? m[2]).trim();
      if (/^(https?:)?\/\/|^(#|data:|mailto:|tel:|javascript:|blob:)/.test(p)) continue;
      const clean = p.split(/[?#]/)[0];
      if (clean && !existsExact(resolve(from, clean)))
        add('R1', lineOf(m.index), `битый путь ассета: «${clean}»`);
    }
  }
  if (isHtml(f)) {
    const back = /<img[^>]+src="[^"]*back_24\.svg"/i.exec(src);
    if (back) add('R2', lineOf(back.index), 'кнопка «назад» через <img back_24.svg>');
    if (baseRoot) for (const m of src.matchAll(/(?:src|href)="\.\.\/|url\(\s*['"]?\.\.\//g))
      add('R7', lineOf(m.index), 'на странице с <base href="../"> «../» лишний');
    if (!/\.partial\.html$/.test(f) &&
        !/<meta name="robots" content="noindex, nofollow">/.test(src))
      add('R16', 1, 'страница без запрета индексации (I8)');
  }
  for (const m of src.matchAll(/#[0-9a-fA-F]{3,8}\b|rgba?\(|hsla?\(/g))
    add('R9', lineOf(m.index), 'литерал цвета — возьми токен из tokens.css');
  const SPACE = { 4: '--space-1', 8: '--space-2', 12: '--space-3',
                  16: '--space-4', 20: '--space-5', 24: '--space-6' };
  if (isStyleish(f)) for (const m of src.matchAll(/(?:padding|margin|gap)\s*:\s*(\d+)px/g))
    if (SPACE[m[1]]) add('R10', lineOf(m.index), `${m[1]}px → var(${SPACE[m[1]]})`);
  if (/\.(html|js)$/.test(f)) for (const m of src.matchAll(/https?:\/\/[a-z0-9.-]+/g))
    add('R15', lineOf(m.index), `внешняя сеть в рантайме: ${m[0]} (I7)`);
  if (inManifestScope(f) && !/^\s*\*\s*DS-Origin:/m.test(src))
    add('R12', 1, 'файл ДС без объявленного происхождения');
  if (f === 'ds/index.css')
    for (const m of src.matchAll(/@import url\('(?!\.\/)([^']+)'/g))
      add('R14', lineOf(m.index), `точка входа ДС импортирует ${m[1]} (I3)`);
  // R3, R4, R6 — по схеме из таблицы, тот же контракт add(rule, line, msg).
  return out;
}

/** Диффовые правила: про действие, а не про состояние. В baseline не попадают. */
function scanDiff(f, src) {
  const out = [];
  const add = (rule, line, msg) => out.push({ rule, line, msg });
  const added = addedLines(f);

  if (isHtml(f)) {                                   // R11
    const s = src.split('\n');
    const starts = s.map((l, i) => /FEED:START/.test(l) ? i + 1 : 0).filter(Boolean);
    const ends   = s.map((l, i) => /FEED:END/.test(l)   ? i + 1 : 0).filter(Boolean);
    for (const [ln] of added)
      if (starts.some((a, i) => ln > a && ln < (ends[i] ?? Infinity)))
        add('R11', ln, 'генерируемый блок — правка будет затёрта');
  }
  const origin = /^\s*\*\s*DS-Origin:\s*(\S+)/m.exec(src)?.[1];   // R13
  if (origin === 'upstream' || origin === 'upstream+local') {
    const patch = /^\s*\*\s*DS-Local-Patch:([\s\S]*?)\*\//m.exec(src)?.[1] ?? '';
    const ranges = [...patch.matchAll(/(\d+)-(\d+)/g)].map((m) => [+m[1], +m[2]]);
    for (const [ln] of added)
      if (!ranges.some(([a, b]) => ln >= a && ln <= b))
        add('R13', ln, 'правка апстримного файла вне объявленных дописок (I2)');
  }
  return out;
}

// ─────────────────────────────────────────────────────────────────────────────
const prev = existsSync(BASELINE) ? JSON.parse(readFileSync(BASELINE, 'utf8'))
                                  : { counts: {} };
const targets = REBASE ? allFiles() : changedFiles();
const counts = {};                     // rule -> file -> n  (текущий замер)
const violations = [];                 // диффовые срабатывания
const grown = [];                      // (rule, file, было, стало)

for (const f of targets) {
  const src = readFileSync(join(ROOT, f), 'utf8');
  for (const v of scanFile(f, src)) {
    (counts[v.rule] ??= {})[f] = (counts[v.rule][f] ?? 0) + 1;
  }
  if (!REBASE) violations.push(...scanDiff(f, src).map((v) => ({ ...v, file: f })));
}

// Рост считается по КАЖДОМУ отсканированному файлу; отсутствие в baseline = 0.
for (const [rule, files] of Object.entries(counts))
  for (const [f, n] of Object.entries(files)) {
    const was = prev.counts?.[rule]?.[f] ?? 0;
    if (n > was) grown.push({ rule, file: f, was, now: n });
  }

if (REBASE) {
  if (grown.length) {
    for (const g of grown)
      console.log(`ДОЛГ ВЫРОС ${g.rule} ${g.file}: было ${g.was}, стало ${g.now}`);
    process.exit(1);
  }
  writeFileSync(BASELINE, JSON.stringify(
    { ...prev, generated: new Date().toISOString().slice(0, 10), counts }, null, 2) + '\n');
  const total = Object.values(counts).flatMap((o) => Object.values(o))
                      .reduce((a, b) => a + b, 0);
  console.log(`baseline обновлён: ${total} нарушений в ${targets.length} файлах`);
  process.exit(0);
}

// R8 — один прогон на все изменённые HTML.
const html = targets.filter(isHtml);
if (html.length &&
    spawnSync('node', ['scripts/nbsp.mjs', '--check', ...html], { cwd: ROOT }).status === 1)
  violations.push({ rule: 'R8', file: html.join(' '), line: 0,
    msg: `неразрывные пробелы: node scripts/nbsp.mjs ${html.join(' ')}` });

for (const g of grown)
  console.log(`FAIL ${g.file} [${g.rule}] стало хуже: было ${g.was}, стало ${g.now} — `
            + 'тронутый файл не имеет права ухудшаться (I11)');
for (const v of violations)
  console.log(`FAIL ${v.file}:${v.line} [${v.rule}] ${v.msg}`);

// Улучшение фиксируем сразу: baseline может только убывать.
let shrunk = 0;
for (const [rule, files] of Object.entries(prev.counts ?? {}))
  for (const [f, was] of Object.entries(files))
    if (targets.includes(f) && (counts[rule]?.[f] ?? 0) < was) {
      (prev.counts[rule])[f] = counts[rule]?.[f] ?? 0; shrunk++;
    }
if (shrunk && !grown.length && !violations.length) {
  writeFileSync(BASELINE, JSON.stringify(prev, null, 2) + '\n');
  console.log(`baseline уменьшен в ${shrunk} записях — закоммить docs/baseline.json`);
}

console.log(`\nПроверено файлов: ${targets.length}. `
          + `Ухудшений: ${grown.length}. Нарушений правил действия: ${violations.length}.`);
process.exit(grown.length || violations.length ? 1 : 0);
```

**Как решают снаружи.** ○ [док] `reviewdog` сделан под ту же задачу: «запускается только
для диффа, а не для всей кодовой базы», режимы фильтрации `added/modified lines`,
`diff_context`, `file` — [reviewdog](https://github.com/reviewdog/reviewdog). ○ [док]
`lint-diff` — то же для ESLint ([lint-diff](https://github.com/grvcoelho/lint-diff)).
○ [инж] Почему приходят к baseline-снимку, а не к смягчению правил:
[Adding New Lint Rules Without the Fuss](https://dev.to/dcwither/adding-new-lint-rules-without-the-fuss-34a2),
[Incrementally linting a codebase](https://ryanbrooks.co.uk/posts/2023-02-17-linting-branch-changes.html).
**Не подходит:** сами инструменты — внешние бинарники, а наши правила специфичны
(`__slot-back`, `TAB_ALLOW_OUT`, `<base href>`, `DS-Origin`). Забираем модель, не
инструмент. Отличие нашей механики от их «фильтра по диффу»: **пофайловый счётчик**
позволяет требовать улучшения от тронутого файла, а не только запрещать ухудшение
конкретных строк.

---

### Р6. `.github/workflows/macket-check.yml` — I9

Сверено со стилем существующих workflow: `deploy-pages.yml` использует
`actions/checkout@v4` и блок `permissions`; `mirror-sourcecraft.yml` — русские имена шагов
и `concurrency`.

```yaml
name: Проверка макетов

# Гоняется на каждый PR в main. В репозиторий ничего не пишет: только читает и
# печатает отчёт в лог и в сводку прогона — поэтому работает и для PR из форков,
# где токен даётся только на чтение.

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: macket-check-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: macket-check          # ← это имя указывается в required status checks
    runs-on: ubuntu-latest
    steps:
      - name: Забрать код с историей
        uses: actions/checkout@v4
        with:
          fetch-depth: 0        # без этого не посчитать дифф с base-веткой

      - name: Поставить Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'    # как в Dockerfile (node:22-slim)

      - name: Правила сборки макетов
        run: |
          node scripts/lint-macket.mjs --diff "origin/${{ github.base_ref }}" \
            | tee lint.txt
          {
            echo '## Проверка макетов'
            echo '```'
            cat lint.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Долг по репозиторию не вырос
        run: node scripts/lint-macket.mjs --baseline
```

**Альтернатива: комментировать результат прямо в PR** (`pull-requests: write` плюс шаг с
`github-script`).

| | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| **Отчёт в сводку прогона** | Ниже: один `tee` и запись в `$GITHUB_STEP_SUMMARY` | Полная, прав не требует | Держится: одинаково работает для веток и форков |
| Комментарий в PR | Выше: нужен шаг с API, обновление существующего комментария вместо создания нового | **Для PR из форков токен только на чтение** — шаг будет падать не по делу | Хуже ровно там, где нужнее: у внешних авторов |

**Рекомендую сводку прогона.** Довод: комментарий заметнее, но ломается на форках, а
именно внешние авторы — та аудитория, ради которой всё это строится. Если ответ на вопрос
C — «все работают в ветках этого репозитория», комментарий можно добавить: это плюс один
шаг, не переделка.

Три решения с обоснованием: `permissions: contents: read` без комментария в PR — иначе
чек падал бы у авторов из форков не по делу, а отчёт виден в сводке прогона;
`fetch-depth: 0` — иначе `git diff origin/main...HEAD` не с чем считать; никакого
`git push` из workflow — под `GITHUB_TOKEN` пуш не перезапускает workflow, и обязательный
чек залипает. Отдельный шаг для `nbsp` не нужен: R8 внутри линтера.

**Дырка:** хватает ли Actions-минут на тарифе — **вопрос B**.

**Как решают снаружи.** ○ [док] GitHub: обязательные проверки не дают смержить PR даже
тем, у кого есть право пуша в защищённую ветку
([about status checks](https://docs.github.com/articles/about-status-checks)); типовые
причины «чек не появился» — имя не совпало или чек не запускался на этом коммите
([troubleshooting](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/collaborating-on-repositories-with-code-quality-features/troubleshooting-required-status-checks)).
○ [инж] Для неразработчиков практикуют `CONTRIBUTING.md` с описанием ожидающих проверок
([Writing Practical Contribution Guidelines](https://tenthirtyam.org/dispatches/2026/03/21/writing-practical-contribution-guidelines-for-github-repositories/)).
**Не подходит:** «автофиксить и коммитить в ветку PR» — рекурсия и залипание чека, а для
форков невозможно.

---

### Р7. Разделение точек входа — I3

**Целевые файлы.**

`ds/index.css` — только ДС, ни одного импорта за пределы `ds/`:

```css
@import url('./tokens.css');
@import url('./fonts.css');
@import url('./animations.css');
@import url('./components/icon.css');
@import url('./components/button.css');
@import url('./components/button-inline.css');
@import url('./components/uni-cell.css');
@import url('./components/uni-card.css');
@import url('./components/content.css');
@import url('./components/contents-view.css');
@import url('./components/tooltip.css');
```

`ds-local/index.css` — ДС плюс наши расширения:

```css
@import url('../ds/index.css');
@import url('./components/icon-slots.css');      /* карта __slot-* — наша, не апстрим */
@import url('./components/button-ext.css');      /* positive/negative/destructive/ai-gift */
@import url('./components/type-scale-ext.css');  /* склейка ds-* с селекторами компонентов */
@import url('./components/button-circle.css');
@import url('./components/tag.css');
@import url('./components/promo-banner.css');
```

`app/app.css` — приложение поверх:

```css
@import url('../ds-local/index.css');
@import url('./components/feed-base.css');
/* … остальные продуктовые компоненты … */
:root { --tabbar-height: 49px; }
```

**Пять вещей, которые ломаются молча, если их не сделать вместе с этим:**

1. **`ds-local/components/icon-slots.css` меняет глубину.** Все 30 путей вида
   `url('../assets/icons/…')` из `ds-local/components/` резолвятся в
   `ds-local/assets/icons/`, где иконок нет. Правильный путь — `../../ds/assets/icons/`.
   Пункт «точный префикс `assets/icons/`, 720 вхождений» этот случай **не покрывает**:
   здесь меняется не префикс, а число уровней вверх.
2. **`new-vision/new-vision.css:17`** — `@import url('../index.css')`, прямая ссылка на
   точку входа ДС мимо любой схемы `app.css`. Становится `@import url('../app.css')`.
3. **Пять страниц `koleso/*.html`**, каждая на строке 10 — `<link rel="stylesheet"
   href="../index.css">`. Становится `href="../app.css"`.
4. **Шесть страниц из D-9** переводятся на общую точку входа: `new-vision.html`,
   `start-lenta.html`, `q3-view.html`, `activity-lenta/view.html`,
   `events-lenta/view.html` подключают `components/app-launch.css` и
   `components/lockscreen.css` поштучно; `index.html` — мета-редирект без стилей, ему
   точка входа не нужна.
5. **`preview.html` расщепляется.** `ds/preview.html` — только примитивы ДС;
   `app/preview-app.html` — продуктовые компоненты, которые сегодня живут в разметке
   витрины (`feed-memory` 701, `feed-base` 728 и 891, `feed-discussion` 759,
   `feed-questions` 793). Без этого приёмочный тест из 1.1 не выполним: витрина откроется
   с неотрисованной половиной, и критерий начнёт врать.

**Развилка внутри Р7: что делать с `tokens.css`, который знает о компонентах (D-4).**

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| A. Разрезать: значения в `ds/tokens.css`, классы в `ds/typography.css`, склейку — в файлы компонентов | Ниже: одна механическая правка, результат чище на вид | Полная | **Отрицательная:** даёт гарантированно непустой дифф с апстримом навсегда, в самом важном файле. Каждый будущий апдейт ДС начинается с ручного разбора нашего разреза |
| **Б. Перекрыть оверрайдом** в `ds-local/components/type-scale-ext.css`, подключаемым после `ds/index.css` | Выше: нужно понимать каскад и держать оверрайд в актуальном состоянии | Полная | Держится: апстримный артефакт не тронут, право на пустой дифф сохранено; при апдейте ДС конфликтует только наш оверрайд, и это видно |

**Рекомендую Б.** Довод — I2 и вопрос 1: пока есть хоть какая-то перспектива получить
доступ к апстриму, право на пустой дифф `tokens.css` стоит дороже, чем красота файла.
Если ответ на **вопрос 1** будет «доступа не будет никогда», вариант A становится дешевле
и его стоит вернуть — это записано и в списке нерекомендуемых идей.

---

### Р8. Слой публичных адресов — I4

**Что он решает архитектурно.** Сегодня адрес прототипа == путь файла на двух каналах из
трёх, поэтому файловая раскладка неприкосновенна, и любое структурное улучшение
оплачивается поломкой чужих ссылок. Правильный ответ — не «не двигать файлы», а
**отвязать адрес от пути один раз**, после чего перемещения бесплатны.

**Источник истины — `docs/urls.json`. Он существует в двух состояниях, и переезд
приложения — это изменение значений в одном файле.**

До волны 4 (файлы на сегодняшних местах):

```json
{
  "/q3":       "/q3-view.html",
  "/activity": "/activity-lenta/view.html",
  "/events":   "/events-lenta/view.html",
  "/nv":       "/new-vision/lenta.html",
  "/preview":  "/preview.html"
}
```

После волны 4 (приложение и ДС разъехались):

```json
{
  "/q3":       "/app/q3-view.html",
  "/activity": "/app/activity-lenta/view.html",
  "/events":   "/app/events-lenta/view.html",
  "/nv":       "/app/new-vision/lenta.html",
  "/preview":  "/ds/preview.html"
}
```

Это и есть смысл Р8: **переезд файлов перестаёт быть событием для внешнего мира и
становится правкой пяти строк.** Р8 обязан идти до волны 4 — иначе заглушки будут вести
в никуда, а `server.mjs` начнёт отдавать 404 по адресам, которые сегодня работают.

**Три подхода к тому, чтобы адреса пережили переезд.**

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| A. Не двигать файлы | Нулевая | Полная | **Отрицательная:** любое структурное улучшение навсегда заблокировано; с ростом числа прототипов раскладка только костенеет |
| Б. Красивые адреса только на VM (как сейчас) | Нулевая, уже сделано | Полная | Слабая: два канала из трёх остаются с адрес == путь, то есть проблема не решена там, где реально ходят по ссылкам |
| **В. `urls.json` + статические заглушки + `server.mjs` из того же файла** | Средняя: один генератор и правка `server.mjs`; неочевидное место — относительные пути внутри заглушки, их проверяет R1 | Полная: заглушка — статический HTML, работает на любом канале | Лучшая: новый прототип = строка в JSON; переезд файлов = правка значений; каналов может стать четыре — механизм тот же |

**Рекомендую В.** Довод: это единственный вариант, после которого перемещение файлов
перестаёт быть событием для внешнего мира. Он же снимает довод «нельзя чинить структуру,
потому что сломаются ссылки» — не обходит его, а устраняет причину.

**Генератор заглушек** для каналов, которые не умеют роутинг:

```js
#!/usr/bin/env node
/**
 * build-redirects.mjs — из docs/urls.json делает статические заглушки
 * для GitHub Pages и SourceCraft Sites. server.mjs читает тот же файл.
 */
import { readFileSync, mkdirSync, writeFileSync } from 'node:fs';
import { dirname, resolve, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '..');
const map = JSON.parse(readFileSync(join(ROOT, 'docs/urls.json'), 'utf8'));

for (const [pretty, target] of Object.entries(map)) {
  const out = join(ROOT, pretty.replace(/^\//, ''), 'index.html');
  mkdirSync(dirname(out), { recursive: true });
  // Относительный путь от папки заглушки к цели: заглушка лежит на один
  // уровень глубже корня, поэтому «..» + target. rel-путь проверяется линтером R1,
  // поэтому canonical с несуществующим адресом сюда класть нельзя.
  const rel = '..' + target;
  writeFileSync(out,
    '<!DOCTYPE html><html lang="ru"><head><meta charset="UTF-8">\n'
    + '<meta name="robots" content="noindex, nofollow">\n'
    + `<meta http-equiv="refresh" content="0; url=${rel}">\n`
    + `</head><body><a href="${rel}">${target}</a></body></html>\n`);
  console.log(`${pretty}/ → ${target}`);
}
```

`<link rel="canonical">` в заглушках намеренно нет: он указывал бы на тот же адрес, что и
`refresh`, не давая ничего, а линтер R1 обязан проверять каждый локальный путь — лишняя
ссылка означала бы лишнее место для рассинхрона. `server.mjs` перестаёт хранить карту в
коде и читает `docs/urls.json` — один источник, три канала, ноль расхождений.

---

### Р9. Защита `main` — I9

| Настройка | Значение | Почему |
|---|---|---|
| Branch name pattern | `main` | с неё публикуются все три канала |
| Require a pull request before merging | ✅ | иначе проверкам негде запускаться |
| └ Required approvals | **0** | ревью здесь не про второго человека, а про то, чтобы прогнать чек |
| Require status checks to pass | ✅ | |
| └ Required check | **`macket-check`** | ровно `jobs.check.name` из Р6; при переименовании job чек молча перестанет быть обязательным |
| └ Require branches up to date | ☐ | при одном авторе даёт лишние ре-раны |
| Require linear history | ☐ | на задачу не влияет |
| **Do not allow bypassing the above settings** | **✅ — ставить** | см. ниже |
| Allow force pushes / deletions | ☐ / ☐ | |

**Развилка: оставлять ли владельцу право обхода.**

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| A. Галка снята — админ обходит защиту (поведение GitHub по умолчанию) | Нулевая | Полная | Слабая: I9 выполняется для всех, кроме одного человека, который вносит больше всего правок. С ростом команды исключение придётся объяснять каждому новому автору |
| **Б. Галка стоит — обхода нет ни у кого** | Нулевая в настройке; выше в привычке: хотфикс идёт через ветку и PR | Полная. **Дырка:** тонкая настройка bypass-списка для отдельных акторов доступна, по обсуждению в GitHub Community, только организациям — вопрос B; на решение это не влияет | Лучшая: правило без исключений не требует объяснений и не размывается |

**Рекомендую Б.** Довод: хотфикс при включённой галке — это ветка, PR с нулём аппрувов и
один автоматический чек, то есть примерно полторы минуты, из которых содержательная часть
— прогон линтера, нужный в хотфиксе больше, чем в обычной правке. Дыра «админ может всё»
срабатывает ровно тогда, когда ей пользуются в спешке, — то есть когда цена ошибки
максимальна. Это не запрет варианта A: если окажется, что деплой приходится чинить ночью
и минуты решают, галку можно снять — но тогда это осознанное исключение из I9, записанное
в самом инварианте, а не найденное потом как дырка.

Перестанет работать приём «пнуть деплой пустым коммитом прямым push» — следы его в
репозитории есть (`.deploy-trigger`, «redeploy trigger 4»). Замена — `workflow_dispatch`,
он в `deploy-pages.yml` уже объявлен.

**Дырка:** тонкая настройка bypass-списка для конкретных акторов — **вопрос B**: по
обсуждению в GitHub Community акторов можно добавлять, только когда репозиторий
принадлежит организации, а `github.com/designodn/proto-moon` выглядит личным. На решение
выше это не влияет: описанная конфигурация работает и на личном репозитории.

**Как решают снаружи.** ○ [док] GitHub: «по умолчанию ограничения branch protection не
применяются к пользователям с admin-правами», и это отдельная опция
([about protected branches](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)).
○ [форум] Про ограничения bypass-списков —
[GitHub Community #43460](https://github.com/orgs/community/discussions/43460) (обсуждение,
не документация). **Не подходит:** корпоративное «≥1 обязательный ревьюер» — при одном
авторе оно либо блокирует работу, либо обходится.

---

### Р10. Контракт генератора — I6, I12

Требование к каждому из девяти генераторов:

1. **`--offline`** — реген из скачанного JSON, без сети. Сегодня у 4; остальным пяти
   дописывается по их образцу.
2. **`--check`** — прогон без записи, ненулевой код выхода при расхождении.
3. **Идемпотентность** — два прогона на одних данных дают побайтово одинаковый результат;
   иначе реген нельзя проверить в CI.
4. **Валидация входа против схемы** — генератор читает схему листа и **падает** на
   нарушении, а не запекает битую карточку. Сегодня это не делает ни один:
   `data/q3-feed.schema.json` и `data/feed.schema.json` существуют, но слова `schema` нет
   ни в одном скрипте в `scripts/` (проверено). Схема есть у 2 лент из 9 — остальные
   пишутся по образцу.
5. **Ничего не пишется в HTML вне маркеров** `FEED:START`/`FEED:END`.

**Три оси.** *Сложность* средняя и неравномерная: `--offline` и `--check` дописываются по
образцу четырёх готовых генераторов, а вот валидация против схемы — новая работа, и схем
сегодня две на девять лент. *Реализуемость* полная: ничего внешнего, JSON Schema можно
проверять вручную написанным валидатором без npm-зависимости. *Масштабируемость* — то,
ради чего это делается: каждая следующая лента получает ворота бесплатно, а без них
каждая новая лента добавляет новый способ запечь битую карточку. Альтернатива —
валидировать не в генераторе, а линтером по результату — хуже по всем трём осям: битую
карточку надо ловить до запекания, а не после.

После этого R11 («не править внутри маркеров») становится жёстким без оговорок: если
содержимое между маркерами восстанавливается офлайн одной командой, ручная правка там —
однозначно ошибка.

---

### Р11. Вопрос ревьюера про дописку в апстрим — I2

**Поправка к постановке:** правила в `.claude/agents/macket-rules-checker.md` **не
пронумерованы** — это маркированный список. Проверено: раздел `## Чек-лист` (строка 70)
содержит буллеты на строках 72, 79, 96, 100, 106, 109, 115, 117; раздел `### Частые грабли
из SESSION-INSIGHTS` (строка 122) — на 124, 126, 131, 136. Вставка описывается якорем.

**Куда:** в раздел `## Чек-лист`, между буллетом «**Генерируемые блоки.**» (строка 115) и
буллетом «**Интерактив проверен в браузере.**» (строка 117).

**Текст буллета:**

```markdown
- **Дописка в апстримный компонент — спроси и зафиксируй.** Триггер: дифф трогает файл,
  у которого в шапке `DS-Origin: upstream` или `upstream+local` (сводка —
  `docs/ds-manifest.md`), и затронутые строки не входят ни в один диапазон из
  `DS-Local-Patch` этого файла. Задай родителю дословно один вопрос:

  > «Файл `<file>` помечен как апстримный (`DS-Origin: <origin>`). Правка в строках
  > `<диапазон>` — наша дописка поверх ДС. Оставляем у нас, отдаём в апстрим или
  > откатываем? Ответь: «в ds-local» / «в апстрим» / «откатить».»

  Принимаются ровно три ответа:
  - **«в ds-local»** → правка переезжает в `ds-local/components/<имя>-ext.css`; в шапке
    апстримного файла `DS-Local-Patch` остаётся без новой записи. Проверь, что переехала.
  - **«в апстрим»** → в отчёт строкой `UPSTREAM-CANDIDATE <file>:<диапазон>`; правка
    остаётся, в `DS-Local-Patch` появляется запись с пометкой «кандидат наверх».
  - **«откатить»** → FAIL, правка не должна попасть в коммит.

  Правка апстримного файла без одного из трёх ответов — **FAIL**: она делает будущий
  апдейт ДС конфликтом (I2). Дублирует и дополняет детерминированное правило R13 линтера:
  R13 ловит сам факт, агент выясняет намерение.
```

**Три оси.** *Сложность* низкая: один буллет в существующий чек-лист. *Реализуемость*
полная. *Масштабируемость* ограниченная и это надо знать: агент работает только там, где
его запускают, то есть у владельца в Claude Code. Поэтому идея подана **поверх**
детерминированного R13, а не вместо него: R13 ловит сам факт правки апстримного файла в
CI у любого автора, агент выясняет намерение там, где он есть. Альтернатива «только
агент, без R13» отвергнута именно по масштабируемости — она воспроизводит ровно ту дыру,
которую закрывает I9.

**Как решают снаружи.** ○ [док] `CODEOWNERS` — штатный механизм «этот путь чужой»
([about code owners](https://github.com/github/docs/blob/main/content/repositories/managing-your-repositorys-settings-and-features/customizing-your-repository/about-code-owners.md));
○ [инж] опыт эксплуатации и почему поверх понадобились боты
([FullStory](https://www.fullstory.com/blog/taming-github-codeowners-with-bots/));
○ [док] `Danger JS` ([danger.systems/js](https://danger.systems/js/)).
**Не подходит:** `CODEOWNERS` требует второго человека — владельца пути; владелец нашего
апстрима сидит в закрытом GitLab. Для контрибьюторов извне он ведёт себя не так, как
ожидают ([renovate#7927](https://github.com/renovatebot/renovate/issues/7927), ○ [форум]).

---

### Р12. Уход из Figma: что теряем и что не теряем

Дизайнеры хотят отказаться от Figma. Этот раздел не о том, как с ней сверяться, — он о
том, чего мы лишаемся, и что из этого было ценным на самом деле. Замеры сохранены
намеренно: они показывают, что **уход теряет заметно меньше, чем кажется.**

**Что измерено.** Из 427 уникальных имён `tokens.css` механически сопоставились с Figma
**9**; из них **2 разошлись**: `$static-surface-status-accent` (`#ff7700` в Figma против
`#F64A00` в `tokens.css:494`) и типографика `scaled/caption-s` (11/14, `robotoflex` против
`.ds-caption-s` 11/16, OK Sans, `tokens.css:121`). Потолок механической сборки — **~2%**
(11 значений из 509). Причины потолка тоже замерены: `get_variable_defs` работает от
конкретной ноды и отдаёт один текущий режим (тёмная тема и `theme-cocktail` —
159 объявлений — не восстанавливаются в принципе); слой `--base-*` (205 объявлений) не
отдаётся, Figma резолвит алиасы до верхнего уровня; шкала `--space-*`/`--font-size-*`
(48 объявлений) в Figma живёт в другой онтологии (`$const-custom-cp-8`, `$custom-cr-999`).

**Что мы теряем, уходя.** Практически ничего из автоматики: сверка покрывала 2% токенов и
два компонента, у которых в комментарии сохранился полный `fileKey`. Автосинхронизация не
работала и не заработала бы — не из-за нашей лени, а из-за формы данных.

**Что мы теряем по-настоящему.** Единственный внешний наблюдатель, который мог показать,
что `tokens.css` разошёлся хоть с чем-то. После ухода из Figma **дифф с апстримом в
GitLab остаётся единственным способом увидеть расхождение ДС.** Это переводит вопрос про
read-доступ к GitLab из «самого ценного ответа» в **единственный**: без него у нас нет
никакого способа узнать, что слепок устарел, — ни автоматического, ни ручного.

**Что делать с существующими ссылками на ноды.** В коде их порядка трёх десятков: по
замеру «строка со словом `figma` плюс id-подобный токен» — **29 уникальных id в 33 файлах,
44 вхождения** (более широкий шаблон даёт больше, но ловит ложные — например
`photo-1543007631-283050bb3e8c`, id фотографии Unsplash в `lenta.html:873`). Полный
`fileKey` есть только в двух файлах: `components/tag.css:3`
(`vTg3KAvmAXA9LigeyiqCWL#10248:46639`) и `components/button-circle.css:3`
(`LYZAXE33pUg2cSL5mmUHYq#7306:60106`). `components/inapp-push.css:11` в этот список не
входит — там библиотека названа словами, ключа нет.

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| **A. Сохранить как исторический след** — оставить комментарии как есть, зафиксировать в `docs/structure.md`, что это след происхождения, а не адрес | Минимальная: одна строка в правилах | Полная | Нейтральная: мусора не добавляет, но и не убывает; новые файлы такие ссылки уже не заводят |
| Б. Вычистить все упоминания | Низкая механически, но требует решения по каждому файлу | Полная | Теряем последнее свидетельство, откуда компонент взялся, — а другого свидетельства у 71 файла нет вовсе |
| В. Заменить на живую ссылку в новом инструменте | Неизвестна: инструмент не назван | **Упирается в решение дизайнеров** — вопрос 2 | Потенциально лучшая, если новый источник вообще заведётся |

**Рекомендую A, разложенный на три разных решения** — потому что «ссылки на Figma» это
три разные вещи, и одинаково поступать с ними неверно:

1. **Значение `DS-Origin: figma` упраздняется.** Указывать на выводимую из эксплуатации
   систему как на *источник* нельзя: инструмент, читающий провенанс, будет пытаться её
   резолвить. Три файла (`button-circle`, `tag`, `promo-banner`) переходят в `local` — и
   это буквально верно: аналога в апстриме мы не знаем, макета не будет, CSS и есть
   артефакт. Побочный эффект правильный: они автоматически становятся кандидатами наверх
   (Р18).
2. **Два полных ключа сохраняются как археология, вне провенанса:**
   `DS-Archive: figma:vTg3KAvmAXA9LigeyiqCWL#10248:46639 (Figma выведена из эксплуатации
   <дата>)`. Именно `DS-Archive`, а не `DS-Source`, — чтобы ни один инструмент не пытался
   их открыть.
3. **Голые id остаются прозой в комментариях и не трогаются.** Это единственный след того,
   что, например, `padding: 12px` в `onboarding-marathon.css:217` было решением, а не
   случайностью. Правило переформулируется с «новое упоминание обязано нести полный ключ»
   на **«новых упоминаний Figma не появляется; существующие не удаляются»** — это правило
   R26.

Вычистить всё (Б) — значит стереть единственный след у части файлов ради косметики.
Вариант В лучше, но зависит от вопроса 2; переход A → В механический, формат поля уже
есть.

**Чего больше нет в документе.** Раздел про Code Connect снят целиком — вместе с ним
снимается и вопрос про права на публикацию в корпоративной библиотеке. Агентская
процедура сверки токенов снята: строить датчик, который умрёт вместе с инструментом,
незачем. Процедура сохранена в истории документа и восстановима, если Figma останется.

### Р12а. Где живёт дизайн-источник правды после ухода из Figma

Это главный вопрос, который открывает уход, и он больше, чем вопрос про инструмент: от
ответа зависит, чем вообще является этот репозиторий. Три варианта по трём осям. Заказчик
про новый инструмент не говорила, поэтому третий вариант описан как рамка, а не как
предложение.

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| **1. Прототип сам становится источником правды.** Репозиторий перестаёт быть «прототипом» и становится дизайн-артефактом | **Высокая, и не в коде.** Меняются требования ко всему: витрина ДС из удобства становится обязательной и должна покрывать все компоненты; провенанс (I1) и структура (I3) — критичны, а не желательны; появляется новый основной сценарий — передача фронтам, а значит нужен формат этой передачи. Неочевидное место: сегодня витрина не покрывает продуктовые компоненты и сама держит их разметку (Р7) | **Полная, ни от кого не зависит.** Всё, что нужно, уже в репозитории: статика, витрина, токены, компоненты. Это единственный вариант, который не упирается в чужое решение | Держится, пока правки вносит команда. Ломается на «другой БЮ»: источник правды, лежащий в репозитории одного продукта, придётся форкать вместе с продуктом. Требует, чтобы `ds/`+`ds-local/` физически отделялись (волна 4) — иначе масштабируемость отрицательная |
| **2. Источник правды — веб-ДС в GitLab**, прототип — потребитель и площадка для новых компонентов | Средняя: механизм уже спроектирован. Путь «кандидат наверх» из `DS-Local-Patch` (Р2, Р11) перестаёт быть побочным эффектом и становится главным рабочим процессом: новое рождается в `ds-local/`, обкатывается в прототипе, уезжает в апстрим | **Упирается в чужое решение целиком** — вопрос 1. Без read-доступа к GitLab невозможно даже увидеть, что источник изменился; без права писать наверх «кандидат наверх» остаётся словом | Лучшая из трёх: один источник на все БЮ, прототипы — потребители. Именно так задумана трёхкорзинная раскладка |
| 3. Другой инструмент вместо Figma | Неизвестна | Неизвестна — **вопрос 2** | Неизвестна |

**Рекомендую 2 как целевой, 1 как рабочий до ответа.** Довод: вариант 2 — единственный,
который масштабируется на другие бизнес-юниты, и вся раскладка раздела 1 уже под него
спроектирована. Но он целиком зависит от доступа к GitLab, которого сегодня нет, поэтому
до ответа на вопрос 1 фактическим источником правды всё равно оказывается прототип —
лучше это признать и выполнить требования варианта 1 (обязательная витрина, провенанс,
структура), чем делать вид, что источник где-то ещё. Хорошая новость: требования варианта
1 — подмножество работ, которые и так стоят в плане (Р7, Р2, волна 4). То есть выбор между
1 и 2 **не меняет ближайших волн** и может быть сделан позже без потерь.

**Что из этого следует для остального документа.** Если источником правды становится
прототип (вариант 1), к приёмочному тесту из 1.1 добавляется второй: **витрина покрывает
все компоненты, которыми пользуется приложение**. Сегодня это не так — `ds/preview.html`
после расщепления покрывает только ДС, а `app/preview-app.html` создаётся из того, что уже
лежит в разметке витрины, то есть покрытие будет неполным по построению. Это отдельная
работа, и она появляется в плане только при ответе «вариант 1».

**Как решают снаружи.** ○ [форум] Что рассинхрон Figma и кода — норма, а не исключение,
видно по живым тредам: [десинк библиотек](https://forum.figma.com/report-a-problem-6/figma-support-not-responding-design-system-components-keep-desyncing-and-breaking-across-files-46684),
[переставшие синкаться режимы переменных](https://forum.figma.com/report-a-problem-6/figma-modes-variables-stopped-syncing-across-files-it-was-working-fine-but-now-stopped-43353).
○ [док] Официальный путь синхронизации у Figma построен на Variables REST API
([figma/variables-github-action-example](https://github.com/figma/variables-github-action-example)) —
то есть на том самом интерфейсе, которого у нас не было; это ещё один довод, что уход
теряет меньше, чем кажется. Материала про «дизайн-систему без дизайн-инструмента, где
источник правды — код» **не нашёл**; если заказчик пойдёт по варианту 1, это не хоженая
тропа, и опереться будет не на что.

---

### Р13. `docs/structure.md` — готовый текст

Правила записаны в полной силе; существующие нарушения не смягчают формулировку — они в
`docs/baseline.json` и в реестре 2.3.

```markdown
# Где что лежит

Единственный документ про структуру. `CLAUDE.md` на него ссылается и не дублирует.
Колонка «чем проверяется» — где правило живёт на самом деле.

| # | Правило | Чем проверяется |
|---|---|---|
| 1 | **Три корзины по происхождению.** `ds/` — апстрим, не правим. `ds-local/` — наши расширения ДС. `app/` — приложение. Файл кладётся в корзину по своему `DS-Origin`, а не по «примитивности». | линтер R12, R13 |
| 2 | **Точка входа одна и однонаправленная.** `ds/index.css` импортирует только `ds/`; `ds-local/index.css` — `ds/` плюс своё; `app/app.css` — `ds-local/` плюс своё. Страница подключает ровно одну точку входа. Поштучный `<link>` на компонент — нарушение. | линтер R14; долг D-9 |
| 3 | **Апстримный файл не правится — вообще.** Ни строк, ни форматирования, ни разрезания на части. Расширение и оверрайд — отдельный файл в `ds-local/`, ссылка на него — в `DS-Local-Patch`. | линтер R13, агент |
| 4 | **Происхождение объявлено.** Каждый файл ДС несёт шапку `DS-Origin` / `DS-Source` / `DS-Snapshot` / `DS-Local-Patch`. `unknown` допустим, отсутствие — нет. Сводка `docs/ds-manifest.md` генерируется. | линтер R12 |
| 5 | **Прототип наследует, а не копирует.** Новый вариант заводится манифестом в `app/protos/<id>/`, а не `cp -r`. Вид объявляется полем `kind`: `build` (свои страницы собираются, свой адрес) или `overlay` (страницы родителя плюс флаг). Ребёнок правит только свою папку; нужен общий компонент — отдельный PR в `ds-local/`. Собранные страницы не редактируются. | линтер R17-R21; долг D-5, D-10, D-16 |
| 6 | **Страница = файл `.html`.** Экраны приложения — в `app/`, отдельный сценарий — в своей папке внутри `app/`. Новых уровней вложенности не заводим. | ревью |
| 7 | **Имена — `kebab-case`, латиница, без пробелов и скобок.** Исключение — `ds/assets/icons/`, где имена приходят из выгрузки ДС. | линтер R5 |
| 8 | **Классы — существующая конвенция:** блок в `kebab-case`, модификаторы `__size-N`, `__style-X`, `__view-X`, `__state-X`, `__slot-X`. Новых схем не вводим. | агент |
| 9 | **JS-компонент — IIFE в `components/*.js`**, подключается `<script src>`. Публичный API — `window.PascalCase`, данные — `window.DS_UPPER_SNAKE`. ESM в браузере не используем. | ревью |
| 10 | **Значения — только из токенов.** Литерал цвета, шага сетки или кегля — нарушение. Существующие в `docs/baseline.json`; тронутый файл не имеет права стать хуже. | линтер R9, R10 + baseline |
| 11 | **Генерируемое — только машинное.** Между `FEED:START` и `FEED:END` правит только генератор. Каждый генератор поддерживает `--offline`, `--check`, идемпотентен и валидирует лист против схемы. | линтер R11; долг D-6, D-15 |
| 12 | **Рантайм локален.** Никаких внешних CDN, шрифтов и картинок — включая те, что подставляются скриптом. Медиа кладёт `scripts/lib/media-cache.mjs`. | линтер R15 + baseline; долг D-7 |
| 13 | **Ни одной битой локальной ссылки.** Любой `src=`/`href=`/`url()` указывает на существующий файл с точным регистром. | линтер R1 |
| 14 | **Ни одной индексируемой страницы.** `<meta name="robots" content="noindex, nofollow">` в `<head>` каждой страницы, `robots.txt` в корне. | линтер R16 |
| 15 | **Публичный адрес объявлен в `docs/urls.json`.** Путь файла контрактом не является; для детей-прототипов источник адреса — поле `url` манифеста, `urls.json` из него генерируется. | ревью |
| 16 | **Файл объявляет, какие классы он даёт** (`DS-Provides`), и чем его контракт расходится с апстримным (`DS-Deviation`). Владелец класса — объявлен, а не выведен грепом. | линтер R22-R24 |
| 17 | **Класс в разметке разрешается либо в `DS-Provides` какого-то файла, либо в `<style>` своей страницы.** Классов «ниоткуда» не заводим. | линтер R25 + baseline; долг D-18 |
| 18 | **Экран передаётся фронту спецификацией** `docs/screens/<screen>.md`, а не ссылкой на папку компонентов. Спека генерируется, руками не пишется. | Р18 |

**Долг.** Правила действуют с первого дня для всего нового. Существующие нарушения
перечислены в `docs/architecture-target.md` §2.3 и посчитаны пофайлово в
`docs/baseline.json`; их число может только уменьшаться.
```

**Три оси.** *Сложность* низкая по написанию и высокая по согласованию: половина правил
фиксирует статус-кво, а правила 2, 5 и 10 сегодня нарушаются и требуют признать долг.
*Реализуемость* полная. *Масштабируемость* — ради неё всё и пишется: это тот текст,
который уедет в скилл для другого бизнес-юнита. Альтернатива — дописать правила прямо в
`CLAUDE.md` — отвергнута: это был бы третий свод поверх существующих двух, и они бы
разошлись.

**Как решают снаружи.** ○ [инж] Ближайшее найденное — практика писать contribution
guidelines так, чтобы структура и требования были понятны до первого PR
([Writing Practical Contribution Guidelines](https://tenthirtyam.org/dispatches/2026/03/21/writing-practical-contribution-guidelines-for-github-repositories/)).
Документов «где что лежит» из дизайн-систем с разбором соблюдаемости **не нашёл**.

---

### Р14. Локальный хук — вспомогательное, не гарантия — I9

Точная строка в существующий блок `scripts` (сейчас `start`, `sync`, `test`):

```json
  "scripts": {
    "start": "node server.mjs",
    "sync": "node scripts/fetch-all.mjs",
    "test": "node --test scripts/lib/*.test.mjs",
    "prepare": "git config core.hooksPath .githooks || true"
  },
```

**`|| true` обязателен — проверено.** `Dockerfile:20-21` делает `COPY package.json ./` и
`RUN npm install --omit=dev || true`, а `.dockerignore:4` исключает `.git`. Внутри образа
`npm install` идёт в каталоге, не являющемся git-репозиторием:

```
$ git config core.hooksPath .githooks    # каталог без .git
fatal: not in a git directory
exit=128
```

**Отдельным пунктом плана — снять `|| true` с `Dockerfile:21`.** Он глушит любые сбои
установки, включая настоящие; после того как `prepare` перестанет валиться сам по себе,
маскировка становится чистым вредом. Это изменение поведения сборки образа, поэтому оно
стоит в плане строкой, а не подразумевается.

**Три подхода к тому, чтобы хук работал не только у владельца.**

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| A. Оставить как есть (SessionStart-хук Claude Code) | Нулевая | Полная | **Отрицательная:** работает ровно у одного человека |
| **Б. `prepare` в `package.json`** | Минимальная: одна строка; неочевидное место — `\|\| true`, без него ломается сборка образа | Полная | Средняя: покрывает всех, кто делает `npm i`; долю таких авторов мы не знаем — вопрос C |
| В. Только CI, без локального хука | Нулевая | Полная | Хорошая по покрытию, но обратная связь приходит через минуты после пуша, а не мгновенно |

**Рекомендую Б вместе с В, а не вместо.** Довод: Б — удобство, которое ловит опечатку до
коммита, но обходится тривиально; гарантией по I9 остаётся CI. Ставить Б вместо В было бы
подменой гарантии удобством.

**Чего приём не закрывает.** Правку через веб-интерфейс GitHub, коммит из GUI-клиента без
node в `PATH`, любого, кто не выполняет `npm i`. Доля таких случаев — **вопрос C**.
Архитектурно локальный хук — удобство; гарантией по I9 является только проверка на стороне
репозитория.

**Как решают снаружи.** ✅ [док] Husky ставит хуки через `prepare` и перечисляет, где это
ломается: в CI и Docker хуки отключают через `HUSKY=0`; GUI-клиенты «запускаются вне
терминала и не инициализируют менеджер версий, оставляя `PATH` без пути к Node»
([how-to](https://typicode.github.io/husky/how-to.html)); первый шаг отладки —
`git config core.hooksPath` ([troubleshoot](https://typicode.github.io/husky/troubleshoot.html)).
○ [инж] Тот же приём без библиотеки —
[dev.to/azu](https://dev.to/azu/git-hooks-without-extra-dependencies-like-husky-in-node-js-project-jjp).
**Не подходит:** сам Husky — новая зависимость ради одной строки `git config`.

---

### Р15. Гигиена дерева

Пути проверены, у всех удаляемых 0 входящих ссылок.

```sh
# Папка с пробелом в конце имени — кавычки обязательны.
git rm -r "assets "
git rm feed-content.json comment-as-feed-twitter.html .deploy-trigger .fig2.png

# Пробелы и скобки в имени. Единственное совпадение по имени в репозитории —
# кусок исходного URL Дзена в data/around-you-media.json:155, целевой файл там другой.
git mv "assets/post_crop_small_1080 (2) 1.png" assets/post-crop-small-1080.png
```

`.fig2.png` упомянут в `.dockerignore:13` — строку убрать тем же коммитом:

```diff
--- a/.dockerignore
+++ b/.dockerignore
@@ -10,7 +10,6 @@
 *.md
 *.sh
 .nojekyll
-.fig2.png
 Dockerfile
 .dockerignore
```

`README.md:10-35` называет четыре несуществующих файла (проверено `ls`) — они заменяются
на реально существующие аналоги:

```diff
--- a/README.md
+++ b/README.md
@@ -27,10 +27,10 @@
     ├── avatar.css              # Avatar (24–56px, image / initials)
     ├── pulse-dot.css           # PulseDot (анимированный индикатор)
-    ├── category-path.css       # CategoryPath (хлебные крошки)
+    ├── breadcrumbs.css         # Breadcrumbs (хлебные крошки)
     ├── feed-base.css           # FeedBase (карточка ленты)
-    ├── feed-tabs.css           # FeedTabs (большие табы сверху)
-    ├── tab-bar.css             # TabBar (нижняя навигация)
-    ├── page-header.css         # StatusBar + NavBar
+    ├── tabs.css                # Tabs (большие табы сверху)
+    ├── tabbar.css              # TabBar (нижняя навигация)
+    ├── header.css              # StatusBar + NavBar
     ├── comment-input.css       # CommentInput (поле + кнопка отправки)
     └── phone-frame.css         # PhoneFrame (375px-viewport для превью)
```

Дифф меняет четыре строки на четыре и ничего не добавляет: блок в `README.md` — выборочный
перечень, а не полный список (неупомянутых существующих компонентов — 66). Задача правки —
убрать ложь, а не воспроизвести дерево; полный перечень генерируется в `docs/ds-manifest.md`
(Р3). После волны 4 весь блок в `README.md` заменяется ссылкой на манифест.

**Три оси.** *Сложность* минимальная, всё проверяемо: у каждого удаляемого файла 0
входящих ссылок. *Реализуемость* полная. *Масштабируемость* обеспечивается не самой
уборкой, а правилом R5, которое не даст новому файлу с пробелом в имени появиться.
Альтернативы нет: оставить мусор — значит публиковать его на трёх каналах и дальше.

Переименование `.png` — изменение пути, поэтому отдельным коммитом.

---

### Р16. Расписание ревизии правил — I9, D-14

Сегодня три файла (`CLAUDE.md:105`, `macket-insights-curator.md:3`, `kirill.md:927`)
утверждают, что правила ревизуются «раз в неделю по крону». Планировщика в репозитории
нет — `grep -rn 'schedule\|cron' .github/workflows/ .sourcecraft/` пуст. Это правило,
которое врёт о себе.

**Решение принято, а не вынесено на развилку:** запуск LLM-агента из CI требует ключа,
наличие которого не подтверждено, поэтому автоматизируется не сам агент, а **напоминание
с чек-листом** — это исполнимо сегодня и не зависит ни от чьего решения.

```yaml
name: Ревизия правил

# Раз в месяц открывает issue с чек-листом ревизии. Сам агент не запускается:
# это делает человек командой /agents macket-insights-curator. Задача workflow —
# чтобы ревизия имела дату, а не подразумевалась.

on:
  schedule:
    - cron: '0 6 1 * *'      # 1-е число, 06:00 UTC
  workflow_dispatch:

permissions:
  issues: write

jobs:
  remind:
    name: rules-review-reminder
    runs-on: ubuntu-latest
    steps:
      - name: Открыть issue с чек-листом
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Ревизия правил — ${new Date().toISOString().slice(0, 7)}`,
              body: [
                '- [ ] прогнать `macket-insights-curator`',
                '- [ ] перенести устойчивые находки в чек-лист `macket-rules-checker`',
                '- [ ] сверить `docs/structure.md` с фактическим состоянием',
                '- [ ] `node scripts/lint-macket.mjs --baseline` — долг не вырос',
                '- [ ] `node scripts/ds-manifest.mjs` — счётчик `unknown` уменьшился',
              ].join('\n'),
            });
```

Тем же коммитом из трёх файлов убирается слово «по крону»: ревизия ежемесячная и
запускается человеком по напоминанию.

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| A. Запускать агента из CI по расписанию | Средняя | **Упирается в ключ API**, наличие которого не подтверждено | Хорошая, если заработает |
| **Б. Напоминание с чек-листом** | Низкая: 25 строк YAML | Полная, ни от кого не зависит. **Дырка:** доступность `actions/github-script@v7` и прав `issues: write` на тарифе — вопрос B | Средняя: ревизия получает дату и след, но выполняет её человек |
| В. Убрать слово «по крону» и не заводить ничего | Нулевая | Полная | **Отрицательная:** правила стареют молча, а именно это и произошло |

**Рекомендую Б.** Довод: из трёх вариантов только он исполним сегодня и не зависит ни от
чьего решения, при этом закрывает главную претензию к текущему состоянию — у ревизии
появляется дата и след, а не подразумевание. Если ключ для варианта A найдётся, переход
механический: тот же `on: schedule`, другой шаг.

---

### Р17. Наследование прототипов вместо копирования — I5, D-5, D-10, D-16

Диагноз в 2.4: расходится не разметка, а данные и негде-лежащие CSS/JS. Отсюда идея —
не «свести два форка», а дать ребёнку три места под дельту и один механизм сборки.

#### Модель: три владельца, непересекающиеся области

У каждого куска байтов ровно один владелец:

| Владелец | Что ему принадлежит |
|---|---|
| **Родитель** | Структура вне генераторных регионов. Родительские страницы остаются рабочими страницами и открываются сами по себе |
| **Генератор** | Всё между `FEED:START/END`, `ACTIVITY:*`, `ACTIVITY-WIDGET:*` |
| **Ребёнок** | Дельта: параметры, именованные блоки, свой CSS, свой JS, конфиг фида и маршрутов — и всё это физически лежит в `app/protos/<id>/` |

**Граница «своё / унаследованное» проходит по папке.** Чего нет в папке ребёнка — то
унаследовано. Сегодня границы нет вовсе: коммит создания `events-lenta` (`b85ff16`)
правил пять общих файлов.

Собранная страница ребёнка — **вывод, а не исходник**. Коммитится (иначе ломаются
`file://`, Pages с `path: '.'` и свежий клон) и помечается баннером
`<!-- PROTO:GENERATED … -->` — по той же конвенции, что уже действует для `data/people.js`.

#### Два вида детей — оба уже существуют в репозитории

| вид | страницы | адрес | пример | когда |
|---|---|---|---|---|
| **build** | порождаются сборкой в свою папку | свой (`/events`) | `events-lenta` | расходится структура, нужен свой контейнер изоляции |
| **overlay** | заимствует страницы родителя в рантайме по флагу; свои — только там, где расходится целиком | адрес родителя + флаг (`?nv=1`) | `new-vision` | расходятся оформление и маршруты, не структура |

Оверлей **уже написан** — просто захардкожен: механика живёт в
`components/screen-transition.js` (файл 291 строка, 15 упоминаний `new-vision`, основной
блок — примерно строки 30-228). Сборки нет вообще. То есть половина модели уже
существует, ей не хватает объявленной формы.

Это же поле закрывает **D-10**: режим изоляции (`<base href="../">` + `proto-contain.js`)
перестаёт быть свойством, которое надо угадывать по содержимому страниц, и становится
полем `kind` в манифесте.

#### Почему сборка, а не рантайм — и это не выбор из вкусов

Замерено: **рантайм-инклюда HTML в репозитории нет вовсе** — все 7 `fetch()` тянут
`assets/lottie/confetti.json` и `data/marathon.json`, ни одного HTML. Данные лежат как
`window.*`-присваивания именно потому, что `fetch` по `file://` блокируется CORS.

А **build-time include уже работает**: `components/today-widgets.partial.html`
инлайнится генератором на сборке — `scripts/fetch-q3.mjs:1690` читает его через
`readFileSync` и вставляет в страницу. *(Правка к исходному проекту: там партиал назван
неиспользуемым — это неверно, единственной ссылкой на него сочли комментарий в
`today-widgets.js:5`. Факт сильнее исходного довода: приём не только допустим, он уже
применяется.)*

| Идея | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|
| A. Копировать папку (как сейчас) | Нулевая в момент создания | Полная | **Отрицательная линейно:** каждое изменение родителя применяется руками N раз; при N=2 уже разошлось на 111 строк разметки |
| Б. Рантайм-инклюд разметки (`fetch` партиалов) | Средняя | **Отрицательная:** CORS по `file://` ломает открытие страницы двойным кликом — способ работы, который в проекте используется | Хорошая, если бы работала |
| **В. Сборка: родитель + дельта → вывод** | Выше обеих: появляется шаг сборки, который можно забыть; нужен позиционный подстановщик и `keepGenerated()` | Полная: `readFileSync`+`writeFileSync`, приём в репозитории уже применяется | Лучшая: N детей стоят столько же, сколько один; изменение родителя доезжает до всех пересборкой |

**Рекомендую В.** Довод: Б отпадает по реализуемости не из-за вкуса, а из-за формата —
в HTML нет декларативного include, а `fetch` недоступен по `file://`. А между A и В
выбор количественный: A линейна по числу детей, В константна.

#### Механизм по слоям

**Разметка — именованные блоки в HTML-комментариях**, поэтому родительская страница
остаётся валидной рабочей страницей:

```html
<!-- BLOCK:navbar -->
  …разметка родителя…
<!-- /BLOCK:navbar -->
```

Для миграции `events` нужно **пять блоков**: в `lenta.html` — `navbar` (заменена кнопка
«Опубликовать»), `tabbar` (убран слот `book`), `tail-scripts` (переставлен
`tabbar-autohide.js`); в `okruzhenie.html` — `navbar` (заголовок, цель «назад», убран
activity-indicator) и `before-list` (добавлены «Опубликовать» и строка сториз). У двух
малых страниц блоков нет вовсе. *(Выведено чтением диффа; сборкой не проверено.)*

**Подстановка параметров — строго позиционная**, три разрешённые позиции: `title` только
внутри `<title>`; `dir` только внутри значений `href`/`src`/`data-href`/`data-target`/
`data-launch` и внутри строковых литералов JS, совпадающих с путём целиком, с якорем на
сегмент; `ssSuffix` только внутри аргумента `sessionStorage.getItem/setItem/removeItem`.
Движок физически не может задеть `class=` — это прямой ответ на `friend-big-card__evl`
(2.4).

**CSS и JS.** `app/protos/<id>/proto.css` (для `events` — 191 строка, вынутая из инлайна)
подключается сборщиком последним `<link>` перед `</head>`, то есть после родительского
инлайнового `<style>`, и перебивает по порядку документа. `proto.js` (96 строк) — перед
`</body>`. Ребёнок **доопределяет** поведение и не отключает родительские скрипты.

**Данные.** Генератор ветвится **по объявленному свойству, а не по имени ребёнка**:
сегодня `IS_EVENTS` встречается в `scripts/fetch-q3.mjs` **15 раз** (проверено; в исходном
проекте было названо 11), плюс `EVENTS_ONLY_POST_IDS` и `scopePosts` — фильтр строк листа,
зашитый в код. Замена — `variant` в манифесте (`activityFrame`, `hideCrumbs`, `tabsSize`,
`cellCta`, `cellLeadSize`) и `excludePostIds` в манифесте **родителя**.

Важное следствие замера: **у детей не разные данные**. В `FEEDS` (`fetch-q3.mjs:63-81`) у
`activity` и `events` один и тот же `gid: '2116709014'`, одно имя листа `lenta-activity`,
один `mediaDir: 'assets/activity'`. Различаются файл выгрузки, `collectionLabel: 'Темы'` и
две исключаемые строки. Модель «общий источник + декларативная дельта» уже фактически
действует — её надо просто назвать.

Отдельно: `scripts/fetch-activity.mjs:519-530` — два регексп-постпроцессора, переписывающих
**уже отрендеренный HTML** ребёнка (`ds-body-m` → `ds-body-l`, `__size-28` → `__size-36`,
перенос кнопки). Это дельта поверх вывода в самой хрупкой форме; становится опциями
рендера `renderCell(a, { leadSize, withRight, cta })` — тем более что `leadSize` и
`withRight` там уже параметры. Шесть захардкоженных целей `spliceFile` (строки 533, 543,
555, 566, 577, 585) становятся циклом по манифестам.

**Рантайм.** `components/tab-bar.js:9-24` — блок под `events-lenta` (`EVENTS_MODE_KEY`,
регексп `/\/events-lenta\//`, удаление слота `book` из DOM) удаляется: у ребёнка свой блок
`tabbar`, слот просто не порождается. `proto-contain.js` читает `TAB_ROUTES`/
`TAB_ALLOW_OUT` из `window.PROTO_MANIFEST`, который сборщик кладёт рядом со страницей как
`manifest.js` — JS-присваивание, а не `fetch`, поэтому работает по `file://`.
`screen-transition.js` теряет имя `new-vision`: `NV_TWIN` → `ownPages`, `SLOT_ROUTE` →
`tabbar.routes`, список CSS → `css`, `SLOTS`/`LABEL` → `tabbar.slots`/`labels`,
`'new-vision.html'` → `homeFallback`, `?nv=1` → `flag`, `__nv` → `htmlClass`; реестр
overlay-детей приезжает генерируемым регионом `/* PROTOS:START */ … /* PROTOS:END */`.

#### Раскладка и манифест

```
app/
  activity-lenta/          ← РОДИТЕЛЬ: рабочие страницы + маркеры BLOCK + proto.json
  protos/
    events-lenta/          ← РЕБЁНОК: только дельта
      proto.json  proto.css  proto.js  blocks/*.html
    new-vision/proto.json  ← вид overlay
  events-lenta/            ← ВЫВОД сборки, адрес /events, баннер PROTO:GENERATED
```

Манифест ребёнка вида `build`: `id`, `kind`, `extends`, `title`, `dir`, `url`, `params`
(три ключа подстановки), `css`, `js`, `pages` (по странице: `bodyAttrs`, `blocks`), `feed`
(`generator`, `sheet`, `gid`, `json`, `mediaDir`, `labels`, `variant`), `activity`,
`tabbar` (`allowOut`, `routes`).

Манифест вида `overlay` (`new-vision`): `kind: "overlay"`, `flag: "nv=1"`, `htmlClass`,
`css`, `ownPages`, `tabbar` (`component`, `slots`, `labels`, `routes`, `slotAlias`),
`homeFallback`. Это ровно те таблицы, что сейчас захардкожены в `screen-transition.js`.

**Поле `url` — источник для `docs/urls.json` (Р8), а не второй реестр адресов.** Карта
адресов генерируется из манифестов; конфликта с I4 нет, наоборот: адрес ребёнка
объявляется там же, где всё остальное про ребёнка.

#### Сборщик — ядро

Полная версия не написана; ниже — три функции, на которых держится корректность.
Синтаксис проверен, поведение сборкой не проверено.

```js
#!/usr/bin/env node
/**
 * build-protos.mjs — собирает страницы детей из родителя и дельты.
 *   node scripts/build-protos.mjs            — собрать всё
 *   node scripts/build-protos.mjs --check    — код 1, если вывод разошёлся с исходниками
 *   node scripts/build-protos.mjs --only=<id>
 * Инвариант: родительские файлы открываются только на чтение.
 */
import { readFileSync } from 'node:fs';

/** Регионы, чьё содержимое принадлежит генератору, а не родителю. */
const GEN = [
  /<!-- FEED:START[\s\S]*?FEED:END -->/g,
  /<!-- ACTIVITY:START[\s\S]*?ACTIVITY:END -->/g,
  /<!-- ACTIVITY-WIDGET:START[\s\S]*?ACTIVITY-WIDGET:END -->/g,
];

/**
 * Переносит содержимое генераторных регионов из предыдущего вывода в свежий.
 * Без этого пересборка затрёт запечённый контент, и порядок «сборка → генераторы»
 * станет обязательным не по соглашению, а под страхом потери данных.
 */
export function keepGenerated(built, previous) {
  if (!previous) return built;
  for (const re of GEN) {
    const mine = [...previous.matchAll(re)].map((m) => m[0]);
    let i = 0;
    built = built.replace(re, (m) => (i < mine.length ? mine[i++] : m));
  }
  return built;
}

/** Замена именованных блоков родителя блоками ребёнка. Отсутствие блока — ошибка. */
export function applyBlocks(src, blocks, dir) {
  for (const [name, file] of Object.entries(blocks)) {
    const re = new RegExp(`(<!-- BLOCK:${name} -->)[\\s\\S]*?(<!-- /BLOCK:${name} -->)`);
    if (!re.test(src)) throw new Error(`нет BLOCK:${name} в родителе`);
    const body = readFileSync(`${dir}/${file}`, 'utf8').trim();
    src = src.replace(re, `$1\n${body}\n$2`);
  }
  return src;
}

/**
 * ПОЗИЦИОННАЯ подстановка: три разрешённые позиции и ни одной больше.
 * class=, свободный текст и любые другие атрибуты недостижимы by construction —
 * это прямой ответ на friend-big-card__all → __evl, случившееся при sed-форке.
 */
export function applyParams(src, p, parent) {
  if (p.title) src = src.replace(/(<title>)[\s\S]*?(<\/title>)/, `$1${p.title}$2`);
  if (p.dir) {
    src = src.replace(
      new RegExp(`((?:href|src|data-href|data-target|data-launch)=")${parent.dir}/`, 'g'),
      `$1${p.dir}/`);
    src = src.replace(
      new RegExp(`(['"])${parent.dir}/([^'"]*)(['"])`, 'g'), `$1${p.dir}/$2$3`);
  }
  if (p.ssSuffix) {
    src = src.replace(
      new RegExp(`(sessionStorage\\.(?:get|set|remove)Item\\(\\s*['"][a-z0-9-]+)-`
                 + `${parent.ssSuffix}(['"])`, 'g'),
      `$1-${p.ssSuffix}$2`);
  }
  return src;
}
```

**Порядок прогона обязателен: `build-protos.mjs` → генераторы.** Он прописывается в
`scripts/fetch-all.mjs` и в `.githooks/pre-commit`; `keepGenerated()` страхует от
нарушения порядка, но не заменяет его.

#### Правила линтера R17-R21

| # | Детекция | Сообщение |
|---|---|---|
| R17 | файл содержит `<!-- PROTO:GENERATED` в первых 3 строках и изменён в диффе | `это собранная страница ребёнка — правь app/protos/<id>/ и пересобери` |
| R18 | `spawnSync('node',['scripts/build-protos.mjs','--check'])` → код 1 | `вывод прототипов разошёлся с исходниками: node scripts/build-protos.mjs` |
| R19 | в диффе одновременно файлы ребёнка и файлы общего/чужого владельца | `правка ребёнка в одном коммите с правкой общего слоя — раздели` |
| R20 | в `components/*.js`, `ds/`, `ds-local/` встречается `dir` любого прототипа вне регионов `PROTOS:START/END` | `общий слой не может знать имя прототипа` |
| R21 | в собранной странице встречается `dir` родителя вне позиций подстановки | `имя родителя протекло в вывод` |

R17, R18, R19, R21 — правила действия (по диффу, вне baseline). R20 — файловое, приходит
с baseline: `screen-transition.js` 15, `tab-bar.js` — блок 9-24, `proto-contain.js` 4 (в
комментариях), плюс по одному в `activity-indicator.js`, `activity-widget.js`,
`invite-list.js`, `moment.js`, `ok-tabbar.js`, `people-data.js`.

**Чего эти правила не ловят прямо:** правку родителя ребёнком. Зато «никто не правит
вывод» (R17) и «вывод воспроизводим» (R18) проверяются детерминированно, а R19 ловит
смешанный коммит — этого достаточно, чтобы нарушение не прошло молча.

#### Миграция `events-lenta`

Ш1 гигиена родителя (убрать из 6 комментариев имя собственной папки) → Ш2 разметить пятью
парами `BLOCK` (на рендер не влияет) → Ш3 вынуть дельту (191 строка CSS, 96 JS, 5 блоков;
часть инлайнового CSS ребёнка — не оверрайды, а правки родительских правил, они становятся
честными оверрайдами) → Ш4 два манифеста → Ш5 перевести данные (15 `IS_EVENTS` →
`variant`, `EVENTS_ONLY_POST_IDS` → `excludePostIds` родителя, постпроцессоры → опции
рендера) → **Ш6 первая сборка и сверка: дифф с текущим `events-lenta/` обязан быть пустым
вне генераторных регионов — главный контрольный шаг** → Ш7 рантайм (`tab-bar.js`,
`proto-contain.js`, `screen-transition.js`) → Ш8 включить `--check` в хук и CI, правила
R17-R21.

`new-vision/` **не мигрируется, а выносится в манифест**: он уже наследник, просто
захардкоженный. Четыре собственные страницы — законная конструкция вида `overlay`.

Выбор вида для нового ребёнка — по одному критерию: нужен ли ему собственный публичный
адрес и свой контейнер изоляции.

#### Запреты и «когда ребёнок перестал быть ребёнком»

Новый прототип не создаётся `cp -r`. Ребёнок не правит ничего вне своей папки — нужен
новый компонент, значит **отдельный PR в `ds-local/` до и независимо от ребёнка**. Ребёнок
не редактирует свои собранные страницы. Генератор не ветвится по имени ребёнка. Общий слой
не произносит имён детей. Ребёнок не отключает скрипты родителя. Подстановка не трогает
свободный текст и `class=`.

**Порог отпочкования** — не число строк, а три признака: блоков-оверрайдов на страницу
больше, чем неоверрайденных; ребёнку нужен блок, которого в родителе нет и который
родителю не нужен; ребёнку нужно другое **поведение** общего компонента, а не другой
конфиг. Тогда отпочкование — объявляемое действие отдельным коммитом: последняя сборка,
снять баннер, влить дельту, убрать `extends`.

#### Риски

Появляется шаг сборки — забыли прогнать, ребёнок молча отстаёт; закрывается `--check`, но
`pre-commit` есть только у владельца, значит реальная гарантия — CI (R18). `git blame` по
собранным страницам теряет смысл — цена любого генерируемого артефакта, репозиторий её уже
платит за `data/people.js`. Конфликты слияния в выводе решаются пересборкой, а не мержем.
Хрупкость подстановки смягчена позиционностью и ловится R21. «Размазывание» ребёнка по
блокам ловится порогом, но порог — правило ревью, а не линтера. Нарушение порядка
сборка→генератор предотвращает `keepGenerated()`, и эта функция не протестирована.

**Не замерено:** трудоёмкость Ш1-Ш8, время сборки, объём полной версии сборщика, поведение
в браузере — ни одна правка не проверена прогоном.

#### Как решают снаружи

- ✅ [док] **WordPress Child Themes** — переопределяешь любой шаблон файлом с тем же
  именем; **`functions.php` ребёнка не заменяет родительский, загружаются оба, дочерний
  первым**. Это ровно наше правило «CSS/JS доопределяют, а не заменяют», и родительская
  тема сама по себе рабочая. **Не подходит гранула:** переопределение целого файла — это и
  есть сегодняшняя копия.
- ✅ [док] **Hugo Theme Components** — два алгоритма слияния: `i18n` и `data` сливаются
  **глубоко по ключу**, `layouts`/`static` — **на уровне файла**. Подтверждает наше
  разделение «данные глубоко (манифест), разметка целыми именованными кусками».
  Ограничение file-level merge (нельзя переопределить часть `baseof.html`) — известная
  проблема; мы её обходим блоками.
- ✅ [док] **Android product flavors** — приоритет source set `demoDebug → debug → demo →
  main`, values сливаются по ключу, ресурсы по имени файла. Точная модель нашей цепочки, и
  она сборочная.
- ○ [инж] **AOSP Runtime Resource Overlay** — «есть оверлей, берём из него; нет — падаем
  на базовый». Это модель `new-vision`.
- ○ [док/инж] **Jekyll Themes**, **Minimal Mistakes** — та же гранула; живой урок
  «копируй только то, что реально меняешь, иначе каждое обновление родителя — ручная
  работа».
- ✅ [инж] **Agoda, один ДС на несколько брендов** — общий компонентный слой плюс дельта
  бренда отдельными JSON-темами, глубокий мерж на сборке, применение через CSS-переменные
  в рантайме. Подтверждает «сборка для структуры, рантайм для оформления». Требует React —
  берём только разделение слоёв.
- **Честное «не нашёл»:** ни одного примера **структурного наследования HTML без сборки и
  без рантайма**. Все механизмы делятся на сборочные и рантаймовые, третьей корзины нет —
  это свойство формата: в HTML нет декларативного include. Declarative Shadow DOM не
  помогает, он даёт слоты для контента, который уже в документе.

---

### Р18. Передача фронтам: единица — экран, а не файл — I13

Раздел отвечает на вопрос, которого в документе не было: **что мы отдаём фронтендеру,
когда прототип готов.** До сих пор вся линия провенанса (Р2, Р3) проектировала границу
между нами и апстримом и жила на уровне **файлов**; передача живёт на уровне **экранов**.

#### Диагноз: проблема не в отсутствии ссылки, а в активной дезинформации

**Шапки врут.** Второй строкой файла в `components/`: **54 файла** из 82 пишут
`OK Design System — <Name>`, **16** — `OK Design System (proto) — <Name>`, 12 — без этого
(проверено; в исходном проекте было 55/16/11). Под вывеской «OK Design System» лежат
`FeedBirthday`, `MeshokUp`, `App Shell`, `FeedQuestions` — заведомо продуктовое.
Фронтендер прочитает это и пойдёт искать готовый компонент в библиотеке. Суффикс
`(proto)` — единственный различитель, и он проставлен непоследовательно: `vvz-card` и
`vvz-portlet` помечены, `meshok-up` и `app-shell` — нет.

**Пространство имён не означает происхождения.** `.uni-cell-text` и `.uni-cell-buttons`
(по 52 употребления) выглядят элементами DS-компонента, но в `components/uni-cell.css` их
нет — они определены в `<style>` внутри `events-lenta/lenta.html:488`.

**Владельца класса нельзя вычислить грепом.** Резолвер «класс → файл, где он определён
правилом в начале строки» на `messages.html` промахнулся дважды: `.button-container` →
`actions-bar.css:75` вместо `button.css:88`; `.tabbar` → `nv-tabbar.css:39` вместо
`tabbar.css`. Причина в том, что продуктовые компоненты пишут правила по DS-классам, и
исход решает алфавит. **Отсюда главный вывод: принадлежность класса файлу должна быть
объявлена, а не выведена.**

**Масштаб «придуманного на месте».** 47 страниц, 45 с собственным `<style>`, 613
инлайн-`style=`. Из классов в разметке **369 разрешаются в `components/`, 377 определены
только в `<style>` своей страницы, 159 не определены нигде**. Уникальных классов с
префиксом `ll-` — 94, в `components/` определено 9. То есть примерно половина визуального
решения — не компонент вообще, и никакая ссылка на ДС этого не покрывает. *(Числа — прогон
архитектора; я их не перепроверял, метод описан в его черновиках `all.mjs`/`spec2.mjs`.)*

#### Чем локальный компонент отличается от апстримного — по тому, что видно

- **Модель другая:** `icon.css:3` — `Source: …/Icon.styl + Icon.tsx`; в апстриме Icon это
  React-компонент с пропом, у нас — CSS-класс плюс локально изобретённая карта 30 слотов
  через `mask-image` (`icon.css:79-108`).
- **DOM другой, и файл это признаёт:** `tooltip.css:5-7` — «в вебе рендерится через portal
  + auto-positioning, в прототипе — простой absolute-блок с теми же модификаторами».
- **Набор вариантов шире, причём расхождение внутри одного файла:** `icon.css:5` в шапке
  заявляет размеры 12/16/20/24/48, а строки 113-124 определяют **двенадцать** (добавлены
  8, 28, 32, 36, 40, 44, 56). У `avatar.css` шапка перечисляет 10, код определяет 11
  (`__size-126 /* legacy */`, строка 79).
- **Модификаторы дописаны внутрь апстримного файла** и по классу неотличимы от
  апстримных — это D-2, увиденное с другой стороны.
- **Токены почти чисто:** 271 уникальная CSS-переменная в `components/*.css`, 188 не
  определены в `tokens.css` — но DS-подобных среди них только **6**; остальные 182 это
  `--button-color`, `--uni-cell-gap` и подобные, то есть **публичный API компонента**, а не
  дырка. Для фронта это ценный артефакт: готовый список точек донастройки.

Итог: совпадает **словарь** (имя блока, `__size-N` / `__style-X` / `__view-X` /
`__state-X`), не совпадают **модель, DOM и набор вариантов**. Это случай, когда «похоже»
опаснее, чем «непохоже».

#### Пять механизмов по трём осям

| | Гранулярность | Сложность | Реализуемость | Масштабируемость |
|---|---|---|---|---|
| **A** поле в шапке (`DS-Origin`/`DS-Source`) | файл | низкая | полная | по компонентам да, **по экранам нет** |
| **B** манифест | файл, запрашиваемо | ~0 сверх A | полная | да |
| **C** `data-ds` руками в разметке | экземпляр + вариант | высокая, **самопротухает** | полная | **отрицательная** |
| **C′** `data-ds` из манифеста в рантайме | экземпляр + вариант | низкая | полная | да |
| **D** спека экрана | экран | средняя | полная | да, растёт с числом экранов |
| **E** вендоринг апстрима | строка | высокая | **нет** — вопрос 1 | наивысшая |

Ключевые доводы. **A не отвечает на задачу**, потому что фронт держит в руках экран: на
`messages.html` — 49 классов из 20 файлов, надо открыть 20 шапок; и файловый уровень **не
выражает вариант** — `.__style-primary` (апстримный) и `.__style-ai-gift` (наш) лежат в
одном файле. **C руками** — дубль `class`, расходится на первой правке и затирается
регеном внутри `FEED`-блоков. **E** невозможно сегодня и даже с доступом не даёт
подключаемых стилей без конверсии из Stylus.

**Рекомендую: A — источник (пишет человек), B, C′ и D — производные (пишет генератор).**
Условие, при котором можно остаться на локальных компонентах, ровно одно и оно не про
компоненты: **каждое отступление от ДС объявлено машинно-читаемо в момент, когда его
делают.** Это и есть инвариант I13.

#### Два новых поля в шапке и одна конвенция

К полям из Р2 добавляются:

1. **`DS-Provides:`** — какие блочные классы файл объявляет своими. **Обязательное**: без
   него резолвер врёт (доказано выше). На нём стоят и C′, и D. Это единственный
   принципиально новый элемент.
2. **`DS-Deviation:`** — чем отличаемся от апстрима **по контракту, а не по строкам**.
   `DS-Local-Patch` фиксирует диапазоны строк — это правильно для линтера R13, но фронту
   бесполезно. Нужна человеческая строка: «тултип absolute вместо portal», «иконка — маска
   вместо инлайн-SVG».
3. **Конвенция без нового поля:** модификаторы, названные внутри `DS-Local-Patch`,
   считаются нашими вариантами — парсер вытаскивает `\.__[a-z0-9-]+` бесплатно.

```css
/**
 * DS-Origin:      upstream+local
 * DS-Source:      design-system/components/Button/Button.styl
 * DS-Snapshot:    unknown
 * DS-Provides:    button-wrapper, button-container, button-content
 * DS-Deviation:   CSS-классы вместо React-компонента; иконка внутри кнопки —
 *                 .icon с mask-image, а не инлайн-SVG
 * DS-Local-Patch: 167-175 …; 177-193 .__style-positive/.__style-negative/
 *                 .__style-destructive; 206-208 .__style-ai-gift
 */
```

#### Три производных артефакта

**1. Спецификация экрана — `docs/screens/<screen>.md`.** Генерируется из `DS-Provides` и
разметки. Прототип генератора написан и прогнан архитектором, состав `messages.html`
настоящий: 49 блочных и элементных классов из 20 файлов, разложенных по четырём корзинам —

- *настоящий веб-ДС, брать готовое:* ButtonInline (`__size-16/20/24`, `__view-secondary`,
  11 экземпляров), Button (`__size-28/36`, `__style-primary`, `__full-width`, 7), UniCell
  (`__state-enabled` + элементы, 7), Icon (`__size-16/20/24`, 7), Content (`content-dot`, 6);
- *ДС с нашими правками:* Icon — карта слотов `__slot-back`/`__slot-close` целиком наша
  (`icon.css:79-108`), размеры 8/28/32/36/40/44/56 сверх апстримных; ButtonCircle —
  `Source: Figma`, то есть не апстрим-код;
- *наше продуктовое:* `message-cell` (7), `vvz-card.__message` (6),
  `vvz-portlet.__messages`, `meshok-up` (владеет ещё `status-bar` и `nav-bar` — отдельных
  файлов нет), `app-shell`/`phone-frame.__fullscreen`, `tabbar.__platform-android`,
  `header.__size-m`, `avatar.__size-56.__type-image` (7), `inapp-pushes`;
- *придумано на странице, компонента нет:* `ll-msg-title`, `ll-messages`, `ll-fab`
  (+`__icon`) — только в `<style>` самой страницы.

**Композиция называется явно** — это главный приём репозитория, и по классу он не
читается. Строка диалога: `uni-cell-wrapper` (ДС) → `uni-cell-container __state-enabled`
(ДС) → `uni-cell message-cell __unread` (**ДС плюс наш модификатор поверх**) → внутри
`avatar` (наш) и `uni-cell-additional-content` (ДС) с `message-cell__name ds-title-s` (наш
элемент плюс ДС-типографика). `message-cell.css:4-5` это уже документирует. Без спеки фронт
здесь ошибётся в обе стороны.

**Токены экрана** — помимо `--space-*` и цветов задействован публичный API компонентов:
`--uni-cell-padding-*`, `--uni-cell-gap*`, `--button-border-radius`, `--button-icon-size`,
`--button-content-gap`, `--overlay-color`, `--avatar-size`, `--avatar-font-size`.

**Честная дырка в спеке:** `DS-Snapshot: unknown` у всех апстримных файлов — фронт обязан
сверить вариант со **своей** актуальной версией библиотеки, потому что мы не знаем, из
какой ревизии снят слепок (вопрос 4).

**2. `components/ds-inspect.js` — замена Figma Dev Mode.** Включается `?ds=1`, читает
сгенерированный `docs/ds-map.json` (`{"uni-cell-wrapper": {"file":"…","origin":"upstream"}}`),
проставляет `data-ds-origin` / `data-ds` / `data-ds-src` и рамку по происхождению.
**Атрибут не хранится в файлах** — поэтому не протухает и не попадает в `FEED`-блоки. Уже
стоящий вручную атрибут не перезаписывается. Пространство свободно: 118 различных
`data-*` уже используется, пересечений нет.

**3. Записка на страницу** — то, чего в спеке быть не может: прототип не продакшен,
тайминги, `phone-frame`, отсутствие адаптива, данные запечены.

#### Правила линтера R22-R26

| # | Детекция | Почему ловит именно протухание |
|---|---|---|
| R22 | нет `/^\s*\*\s*DS-Provides:/m` в файле области манифеста | без поля резолвер угадывает и врёт |
| R23 | класс объявлен в `DS-Provides` двух разных файлов | два владельца одного класса — это разъехавшийся дубль |
| R24 | правило `/^\.([a-z][a-z0-9-]*)/gm`, чей блок не перечислен ни в чьём `DS-Provides` | завели компонент, объявить забыли — самый частый способ протухнуть |
| R25 | класс в `class="…"`, не разрешающийся ни в `DS-Provides`, ни в `<style>` своей страницы | сегодня таких 159; правило не даёт числу расти |
| R26 | появление нового упоминания `figma` в коде | Figma выводится из эксплуатации, новых ссылок быть не должно (см. Р12) |

R22 приходит с baseline (121 нарушение в день ноль, дальше только вниз). **R23 и R24
приходят с нулём сразу** — они ловят только новое.

**Буллет в чек-лист `macket-rules-checker`**, в стиле уже принятого Р11: новый файл в
`components/` / `ds-local/` / `app/components/` — нет `DS-Origin` FAIL, нет `DS-Provides`
FAIL; при `DS-Origin: local` задать родителю дословный вопрос «продуктовое или кандидат в
ДС»; при ответе «кандидат» — добавить `DS-Upstream-Candidate: <дата> — <зачем общий>` и
класть в `ds-local/`; ответа нет — FAIL.

#### 71 файл без провенанса: приоритизация, а не гадание

**Происхождение файла нельзя установить из репозитория** — клон shallow, апстрима нет.
Машинно строится только **очередь вопросов человеку**. Признаки: имя общего UI-примитива
(`header`, `divider`, `media`, `badge`, `picture`, `text-input`, `chips`, `tabs`, `island`,
`breadcrumbs`, `avatars-view`, `pulse-dot`) против продуктового (`vvz-*`, `feed-*`,
`clip-*`, `gift-card`, `marathon`, `koleso`, `meshok-up`) против оболочки стенда
(`phone-frame`, `app-shell`, `lockscreen`, `screen-transition`, `proto-contain`); префикс
варианта (`nv-*`); ширина употребления; DS-семантика в комментарии против описания сцены;
**отсылка к имени Figma-компонента библиотеки** (`avaPicture 1.3`, `notificationToast 1.0`,
`ChipChoice + ChipsView`, `buttonCircle 1.1`, `primary ◇ tag 1.2`) — 6 файлов, сильный
признак; суффикс `(proto)`.

Ширина замерена (экранов / прототипов): общие имена — `island` 15/5, `header` 13/5,
`avatars-view` 9/4, `divider` 9/3, `media` 8/4, `breadcrumbs` 8/4, `tabs` 8/4, `badge` 7/4,
`picture` 7/4, `text-input` 5/2, `pulse-dot` 5/3. Заведомо не ДС, но широкие — `meshok-up`
38/5, `nv-tabbar` 32/5, `actions-bar` 31/6, `app-shell` 31/5, `clip-feed` 30/5,
`phone-frame` 30/5, `post-layer` 29/5, `nv-navbar` 26/5.

**Очередь: около 12 файлов сверху** (общее имя + широкое употребление + DS-семантика) —
один разговор закрывает основной риск.

**Чего нельзя определить машиной вообще:** есть ли аналог в вебе; совпадает ли контракт
при совпадении имени; из какой ревизии слепок; был ли файл апстримным и разошёлся ли.
**`unknown` — рабочее значение, а не поражение; по-настоящему опасен только ложный
`upstream`**, потому что фронт по нему пойдёт в библиотеку и не найдёт компонента.

#### Кандидат наверх — механизм из четырёх шагов

1. **Пометка** `DS-Upstream-Candidate: <дата> — <зачем общий>`.
2. **Триггер, а не добрая воля:** компонент с `DS-Origin: local`, встречающийся на **≥3
   экранах в ≥2 папках-прототипах**, автоматически попадает в раздел «требует решения»
   манифеста. Порог измеряемый — ширина употребления уже считается.
3. **Генерируемая RFC-заготовка** — всё нужное уже в коде: имя и файл, API (блоки из
   `DS-Provides`, модификаторы, CSS-переменные), потребляемые токены (доказательство, что
   компонент собран на ДС), где употребляется с числами, на чём построен композицией,
   скриншот (агент `screenshot-testing` в окружении есть). Человек пишет одно предложение
   «зачем общий».
4. **Доставка упирается в решение не наше:** нужен **названный адресат** на стороне
   веб-ДС и канал. Без этого шаги 1-3 дают аккуратно оформленную свалку. Вопрос 9. Очередь
   кандидатов встаёт пунктом в тот же ежемесячный чек-лист, что и Р16.

#### Что меняет уход из Figma

Уход **меняет выбор в трёх местах**, и все три — в сторону усиления:

- **C′ (`data-ds` в рантайме)** из приятного инспектора становится **заменой Figma Dev
  Mode**: ткнуть в элемент и увидеть, что это за компонент и откуда, — ровно то, что
  уходит вместе с Figma. Переходит из «желательно» в «обязательно».
- **D (спека экрана)** из отчётности становится обязательным выходом: дублировать нечем.
- **B (манифест)** меняет назначение: был реестром происхождения, становится **каталогом
  компонентов** вместо библиотечной страницы. К нему добавляется то, чего нет в Р3, —
  ссылка на живую витрину рядом с каждым компонентом: каталог без картинки дизайнеру
  бесполезен.

**Новый структурный риск, назвать прямо.** Если Figma уходит **и** доступа к GitLab нет,
прототип становится дизайн-системой, у которой одновременно **нечем измерить расхождение**
с веб-ДС (не «не измеряем», а нечем), **некуда отдать** родившийся компонент и **нет
резервной копии решения** — единственный носитель это git-репозиторий с 209 открытыми
ветками, shallow-историей и прямым push в `main`. Единственная точка отказа без дубля.
Инженерно это не закрывается ни одним из пяти механизмов выше. Правильный ответ не
обходной путь, а read-доступ к апстриму — **поэтому вопрос 1 переклассифицирован из
«самого ценного» в блокирующий**.

#### Как решают снаружи

**Берётся.** ✅ [док] **Scottish Government Design System — tracking**: `tracking.init()`
проставляет `data-*` компонентам ДС в рантайме, значение выводится из контекста, **уже
стоящий в разметке атрибут не перезаписывается**, есть `scope`. Единственный найденный
пример именно такой механики в статике — прямой прообраз `ds-inspect.js`. ✅ [док]
**Chromium `README.chromium`** — подтверждает выбор полей. ○ [док] **Norton Design System
RFCs** — RFC как markdown по шаблону, PR, финальный период комментариев; готовая форма для
кандидатов наверх. Репозиторий живой, но малоактивный — честный сигнал, что такие очереди
нуждаются в назначенном владельце (шаг 4 выше). ○ [инж] **Nathan Curtis, Contributions to
Design Systems** — деление вкладов на лёгкие/средние/тяжёлые, обоснование порога «≥3
экрана в ≥2 прототипах». ○ [док] **Custom Elements Manifest** — не подходит как формат (мы
не web components), но подходит **как список полей** для `ds-map.json`.

**Не подходит, с причиной.** ○ [док] **Figma Code Connect**, включая HTML-режим:
технически подошёл бы лучше, чем ожидаешь — код не исполняется, сниппеты обрабатываются
как строки, сборка не нужна, — но требует CLI, доступа к файлу и прав публикации в Figma.
С уходом из Figma вариант **не отклонён, а перестал существовать**, и зафиксировать это
надо именно так. ○ [док] **Figma Variables REST API** — назначает Figma источником истины.
○ [док] **DTCG** (первая стабильная версия 2025.10) — правильная цель, но переводить
токены в неё имеет смысл, только когда есть апстрим, из которого конвертировать
(вопрос 1). ○ **Инструменты трекинга адопшена** (Omlet, Luro, react-scanner, Supernova) —
все на сборке, React-AST или SaaS с доступом к коду, а данные у нас персональные.
**Submodule** — невозможно сегодня по той же причине, что E.

**Не нашёл:** разбора именно нашего сочетания — статический прототип без сборки как
единственный дизайн-артефакт рядом с закрытым корпоративным ДС на другом языке. Все
обсуждения «код как источник истины вместо Figma» предполагают, что код — это подключаемая
библиотека, а не прототип. Наш случай ближе к вендорингу закрытой зависимости, чем к
дизайн-хендоффу.

---

## 4. План перехода

Порядок продиктован зависимостями между решениями, а не их стоимостью. Всё, что
архитектурно правильно, стоит в плане.

### Волна 0 — немедленно

| Что | Решение | Почему первым |
|---|---|---|
| `robots.txt` + `<meta name="robots">` на все 46 страниц | Р1 | вредит прямо сейчас; заодно R16 приходит в волну 1 с нулём |
| Вычистить `SYNC_*`/`GITHUB_TOKEN`/`UPLOADS_*`, шапку `Dockerfile`, установку `git`; удалить `UPLOADS.md` | Р4 | лишние привилегии и ложь конфигурации (D-12) |
| Гигиена дерева и `README.md` | Р15 | дешевле до массовых правок, чем тащить мусор через них (D-11) |

### Волна 1 — правила начинают действовать

| Что | Решение |
|---|---|
| Линтер + первый прогон `--baseline` | Р5 (закрывает D-8 в части ratchet) |
| Workflow `macket-check` на `pull_request` | Р6 |
| Защита `main` с галкой «Do not allow bypassing» | Р9 |
| `prepare` в `package.json` | Р14 |
| **Снять `\|\| true` с `Dockerfile:21`** | Р14 |
| `docs/structure.md` | Р13 |
| Расписание ревизии правил + вычистить «по крону» из трёх файлов | Р16 (закрывает D-14) |

После этой волны I9 выполняется: ни одна правка не попадает в `main` мимо проверок,
независимо от того, чем правит автор.

### Волна 2 — граница ДС и контент становятся объявленными

| Что | Решение | Предусловие |
|---|---|---|
| Шапки `DS-Origin` в 121 файле | Р2 (D-1) | **вопросы 4 и 5** |
| Манифест; R12 и R13 в работе | Р3, Р5 | Р2 |
| Буллет в чек-лист агента | Р11 | Р2 |
| Зафиксировать статус Figma-ссылок как исторического следа в `docs/structure.md` | Р12 | — |
| Схемы для остальных лент; генераторы читают схему и падают | Р10 (D-15) | — |
| `DS-Provides` и `DS-Deviation` в шапки; R22-R24 в работе | Р18 (D-17, D-19) | Р2 |
| Генератор спеки экрана; `docs/ds-map.json`; `ds-inspect.js` | Р18 | `DS-Provides` |
| Очередь из ~12 файлов на разговор о происхождении | Р18 | — |

Работ по сверке с Figma в плане больше нет: дизайнеры уходят из инструмента, строить
датчик, который умрёт вместе с ним, незачем (Р12). Что приходит на замену — зависит от
**вопроса 1**: при ответе «доступ к GitLab есть» в план добавляется зеркало апстрима и
дифф против него; при ответе «доступа не будет» способа увидеть расхождение ДС не остаётся
вовсе, и это надо принять явно, а не обнаружить потом.

### Волна 3 — адрес перестаёт быть путём, точки входа расходятся

| Что | Решение | Предусловие |
|---|---|---|
| `docs/urls.json` (состояние «до») + генератор заглушек; `server.mjs` читает файл | Р8 | — |
| Разделение точек входа; расщепление витрины; перевод `new-vision.css:17`, пяти `koleso/*.html:10` и шести страниц D-9 | Р7 (D-3, D-4, D-9) | Р2 |
| Объявить режим изоляции для `new-vision/` и `koleso/` в `docs/structure.md` и в шапках страниц | Р13 (D-10) | — |

Волна 3 делает волну 4 обратимой. Её нельзя пропустить: без слоя адресов любое
перемещение файлов остаётся ломающим.

### Волна 4 — физическая раскладка и наследование прототипов

Переезд в `ds/` + `ds-local/` + `app/` **и перевод `events-lenta` на наследование**. Оба
шага двигают одни и те же файлы, поэтому идут в одном окне заморозки; порядок внутри
волны: сначала переезд, потом Р17 (иначе сборщик пришлось бы настраивать дважды).

Миграция ребёнка — шаги Ш1-Ш8 из Р17, с контрольной точкой Ш6: **дифф собранного
`events-lenta/` с текущим обязан быть пустым вне генераторных регионов**. `new-vision/` не
мигрируется, а выносится в манифест вида `overlay` — это закрывает и D-10, и D-16.

**Порядок прогона фиксируется здесь же: `build-protos.mjs` идёт до генераторов** — и в
`scripts/fetch-all.mjs`, и в `.githooks/pre-commit`, и в CI.

Способ переезда файлов:

1. **Окно заморозки.** 209 открытых веток; после массового `git mv` каждая живая ветка,
   трогающая HTML/CSS, мержится через rename/modify-конфликты. Нужен полный клон (текущий
   shallow — merge-base доступен у 21 из 210), инвентаризация «слить / закрыть / пометить
   as-is» и согласованное окно. **Вопрос 7.**
2. **Два коммита.** Первый — только `git mv` (сохраняет историю). Второй — замены путей.
3. **Точный паттерн для иконок.** `assets/icons/` → `ds/assets/icons/` (720 вхождений из
   2197); `assets/` остаётся приложением. Замена `assets/` регуляркой по дереву сломает
   1477 путей к медиа. Отдельно — пути внутри `ds-local/components/icon-slots.css`: там
   меняется не префикс, а глубина (`../../ds/assets/icons/`).
4. **Не только HTML/CSS/JS.** `components/` встречается ещё в `data/feed.schema.json`,
   `data/q3-feed.schema.json`, `data/people.json`, `fetch-people.mjs`, `fetch-q3.mjs`,
   `wire-vvz.mjs` и в 87 местах в `.md` (правила, чек-лист агента, память агентов).
5. **`proto-contain.js` не правится.** Путь монтирования нигде не захардкожен
   (`components/proto-contain.js:20` — `DIR` из `location.pathname`, цели резолвятся через
   `new URL(raw, document.baseURI)`); при переносе приложения целиком файл не требует ни
   одной правки. Проверено, на это можно опереться.
6. **`docs/urls.json` переводится в состояние «после»** — пять строк.
7. **Приёмка — линтером и приёмочным тестом 1.1**, не глазами.
8. **Очистить `/opt/proto-moon` перед первым деплоем.** `.sourcecraft/ci.yaml`
   распаковывает архив поверх (`tar -C /opt/proto-moon`) и ничего не удаляет.

### Волна 5 — закрытие долга

| Долг | Что делается |
|---|---|
| D-2 | Дописки `button.css` и карта `__slot-*` переезжают в `ds-local/`; `DS-Local-Patch` очищается |
| D-6 | `--offline` дописывается пяти генераторам (Р10) |
| D-7 | `lottie-web` кладётся локально (включая 7 страниц с инжектом скриптом); **Roboto кладётся в `ds/fonts/` рядом с OK Sans**, `@font-face` — в `ds/fonts.css`: без него локализация `fonts.googleapis.com` бессмысленна, потому что сами файлы шрифтов лежат на `fonts.gstatic.com`; `upload.wikimedia.org`, `okcdn.ru`, `i.pinimg.com` и одиночные хосты — через `media-cache` |
| D-8 (часть ДС) | 138 литералов в `components/*.css` заменяются на токены — **одна работа с владельцем и датой**, результат проверяется витриной; целевое значение — ноль |
| D-16 | Остатки имён детей в общем слое, не снятые волной 4 (`screen-transition.js`, `tab-bar.js`, `fetch-q3.mjs`) — добиваются до нуля по baseline R20 |
| D-18 | 159 классов «ниоткуда» — сокращаются по мере касания страниц, baseline R25 только вниз |

**Отдельно про `pravatar`/`picsum` в D-7.** Прямая замена на `assets/people` неверна: это
решение по I7 увеличило бы объём реальных персональных данных на публичных каналах, то
есть ухудшило бы I8. Правильный ход — **локальные, но сгенерированные лица**: они
закрывают I7 полностью и одновременно снимают остроту I8. Это же снижает риск по
**вопросу 3**: чем меньше в репозитории настоящих лиц, тем меньше значения имеет ответ
про контроль доступа.

### Идеи, которые рассматривались и не рекомендуются

Ни одна из них не «неправильная» — у каждой отрицательна реализуемость или
масштабируемость, и вот в чём именно. Если обстоятельства изменятся, любую можно вернуть.

| Идея | Что с ней не так | При каких условиях вернётся |
|---|---|---|
| **LLM-агент как обязательный status check** | *Масштабируемость отрицательная:* недетерминированный вердикт в блокирующей проверке нельзя воспроизвести и нечем оспорить, поэтому с ростом числа авторов растёт число споров, которые нечем закрыть. Агент остаётся ревьюером с правом задать вопрос человеку (Р11) | Если появится способ получать от агента детерминированный, воспроизводимый вердикт с фиксированным входом |
| **Автокоммит в `main` с раздающего процесса** | *Реализуемость есть, масштабируемость отрицательная:* обходит PR, защиту ветки и линтер, то есть отменяет I9 для всего, что через него проходит. Чем больше контента, тем больше правок мимо ворот | Если контур пересобрать так, чтобы он создавал ветку и PR (Р4) — тогда это не возврат, а другая идея |
| **Figma как источник истины для `tokens.css`** | *Реализуемость отрицательная по двум причинам сразу:* замерянный потолок автосборки ~2%, режимы и слой `--base-*` не отдаются; и дизайнеры уходят из инструмента. Источник — Stylus в GitLab | Если появится инструмент с полноценным экспортом переменных и решение дизайнеров его завести (вопрос 2) |
| **Разрезание `tokens.css` руками** («аккуратно вынести типографику») | *Масштабируемость отрицательная:* даёт гарантированно непустой дифф с апстримом навсегда, в самом важном файле, — то есть ломает I2 ровно там, где он нужнее всего. Тот же эффект достигается оверрайдом в `ds-local/` (Р7) | Если ответ на вопрос 1 — «доступа к апстриму не будет никогда»: тогда терять нечего, и разрезание становится дешевле оверрайда |
| **Смягчение правила ради легаси** (исключения в формулировке вместо baseline) | *Масштабируемость отрицательная:* правило с исключениями «потому что исторически» перестаёт быть правилом на первом же споре, а число исключений растёт быстрее, чем число правок. Легаси идёт в пофайловый baseline (I11, Р5) | Не вернётся: baseline даёт то же послабление, но с обратным знаком — он убывает, а исключения накапливаются |
| **Новый прототип копированием страниц** | *Масштабируемость отрицательная линейно:* каждое изменение ДС применяется N раз вручную и расходится — это уже произошло с `events-lenta/` (D-5, 974 и 544 расхождения) | Не вернётся; для существующей пары нужна отдельная задача с проектным решением |

---

## 5. Вопросы

Два списка. Первый — то, что решаем не мы: внешние блокеры, без ответа на которые часть
архитектуры недостижима. Второй — то, что мы просто спросим: короткие уточнения, не
требующие ничьего согласования.

### 5.1. Решаем не мы

**1. Можно ли получить read-доступ к `gitlab.corp.mail.ru/ok/ODKL/odnoklassniki-frontend-common`?**
*Решение организации.* **После ухода из Figma это единственный вопрос, ответ на который
определяет, есть ли у нас вообще способ узнать, что ДС изменился.** Раньше расхождение мог
показать хотя бы датчик по Figma (2% покрытия, но не ноль); теперь и его не будет. Без
доступа «обновить ДС» не имеет технического смысла: не с чем сравнивать.
— (а) Доступ есть → `ds/` заводится как зеркало апстрима с зафиксированной ревизией,
`DS-Snapshot` становится настоящим, I2 проверяется диффом, а не честным словом. Вариант 2
из Р12а («источник правды — веб-ДС») становится достижимым, и вся раскладка раздела 1
работает так, как задумана.
— (б) Не будет → фиксируем в `README.md` и в манифесте, что ДС — форк без апстрима.
Тогда `ds/` и `ds-local/` остаются разными корзинами только по происхождению, запрет на
правку `tokens.css` (Р7) можно пересмотреть, а источником правды де-факто становится
прототип (вариант 1 из Р12а) со всеми его требованиями.
— (в) Возможен при переезде в GitLab → см. вопрос 8.

**2. Где живёт дизайн-источник правды после ухода из Figma — и что делать со ссылками на
ноды?**
*Решение дизайн-команды.* Разбор трёх вариантов — Р12а; здесь нужен выбор.
— (а) Прототип сам становится источником правды → к плану добавляется обязательное
покрытие витриной **всех** компонентов, которыми пользуется приложение (сегодня это не
так), и формат передачи фронтам.
— (б) Источник — веб-ДС в GitLab → зависит от вопроса 1; путь «кандидат наверх» из
`DS-Local-Patch` становится главным рабочим процессом, а не побочным эффектом.
— (в) Появится другой инструмент → назовите какой, дальше можно оценивать; домысливать не
буду.
*Отдельно, тем же ответом:* порядка трёх десятков ссылок на Figma-ноды в коде (29
уникальных id в 33 файлах по замеру со словом `figma`) — сохранить как исторический след
происхождения (рекомендую), вычистить или заменить на ссылку в новом инструменте.

**3. Какой уровень доступа должен быть у прототипа с персональными данными?**
*Решение владельца данных.* 38 реальных имён и 38 фотографий, три канала публикации.
— (а) Публичный адрес, но не индексируется → Р1 достаточен.
— (б) Доступ по паролю/списку → Pages и SourceCraft Sites отпадают как каналы; остаётся
VM, и `server.mjs` получает слой аутентификации. Волна 0 включает отключение двух каналов.
— (в) Данные обезличиваются → `fetch-people` меняет контракт, `assets/people` заменяется
сгенерированными лицами. Архитектурно самый чистый вариант: снимает вопрос вместе с риском
и закрывает половину D-7.

**4. Из какой ревизии снят слепок ДС и чем конвертирован?**
*Решение того, кто снимал; если не восстанавливается — заказчика.*
— (а) Ревизия известна → `DS-Snapshot` заполняется, дифф с апстримом вычислим, I2
проверяется машинно.
— (б) Утрачено → `DS-Snapshot: unknown` во всех апстримных файлах, и это фиксируется как
принятое состояние: обновление ДС невозможно до восстановления доступа (вопрос 1).

**5. Кто может определить происхождение 71 файла без `Source:`?**
*Решение заказчика — назначить человека.* История shallow, авторство не восстанавливается.
— (а) Автор доступен → Р2 выполняется полностью.
— (б) Частично → `unknown` штатное; счётчик в манифесте показывает остаток.
— (в) Никого нет → разметка только по машинным признакам (11 файлов с `Source:` плюс те,
где есть Figma-нода); I1 выполняется формально, I2 — только для 11 файлов. Это надо
принять сознательно.

**6. Контур загрузки медиа возвращаем?**
*Решение заказчика.* Сегодня он описан в `UPLOADS.md`, заряжен переменными в
`docker-compose.yml` и отсутствует в коде целиком, включая «dual-mode» у `media-cache`.
— (а) Не возвращаем → Р4 в полном объёме.
— (б) Возвращаем → обязательное условие по I10: сервис создаёт ветку и PR, а не пишет в
`main`; отдельная задача.
— (в) Не решено → Р4 всё равно выполняется, а проект контура живёт как предложение.

**7. Что делать с 209 открытыми ветками?**
*Решение заказчика; блокирует волну 4.* Содержимое 188 непроверяемо — клон shallow.
— (а) Закрыть массово → окно заморозки короткое, волна 4 идёт сразу за волной 3.
— (б) Смержить → сначала полный клон и инвентаризация; волна 4 сдвигается.
— (в) Оставить как есть → массовый `git mv` практически исключён, волна 4 выполняется по
частям, папка за папкой.

**8. Насколько реален переезд в GitLab и в каком горизонте?**
*Решение организации.*
— (а) Вероятен → все проверки пишутся Node-скриптами с тонкой обёрткой CI (так и
спроектировано): при переезде переписывается YAML, а не логика.
— (б) Маловероятен → допустима более плотная интеграция с GitHub.
— (в) Состоится вместе с импортом ДС → волна 4 планируется под него, чтобы раскладка была
готова к моменту импорта.

### 5.2. Просто спросим

**A. `.avatar.__size-126` — валидный размер или долг?** Определён в `avatar.css:79` с
пометкой `/* legacy */`, отсутствует и в шапке файла, и в чек-листе агента. (а) валиден →
добавить в шапку, R3 читает файл как есть; (б) долг → убрать из `avatar.css`, заменить
употребления, правило начнёт его ловить.

**B. Какие права и лимиты у репозитория?** Есть ли admin (нужен для Р9), хватает ли
Actions-минут (Р6, Р16), доступен ли `actions/github-script` — проверяется одним взглядом
в настройки, но из сессии не видно.

**C. Как физически работают дизайнеры, которые правят прототип?** Ветки этого
репозитория или форки; есть ли node и делают ли `npm i`; правят ли через веб-интерфейс.
От ответа зависит не архитектура, а формулировки в `CONTRIBUTING.md` и то, стоит ли
рассчитывать на Р14 хоть сколько-нибудь.

---

## 6. Как это проверялось

Все замеры повторены на рабочем дереве коммита `5436c38`. Проверялось **по коду**:
`README.md`, `UPLOADS.md`, комментарии в `Dockerfile`, `docker-compose.yml` и в агентских
файлах рассматривались как заявления, требующие подтверждения, — и три из них не
подтвердились.

**Проверено лично и вошло в документ:** `server.mjs` — 163 строки, `X-Robots-Tag` на 100,
`/robots.txt` на 103, ни `/content`, ни `upload`, ни S3, ни git; отсутствие `robots.txt` и
`<meta name="robots">` (0 вхождений); `assets/people` — 38 файлов, 4 МБ; `data/people.json`
— 38 имён; `docker-compose.yml:15-31`; `Dockerfile:3-7` (шапка), `:14-17` (установка git),
`:20-21` (`COPY package.json` / `npm install … || true`); `scripts/lib/media-cache.mjs` — 0
упоминаний бакета и `UPLOADS_*`; `scripts/lib/bucket.mjs` импортируется только из
`check-bucket.mjs`, `migrate-assets.mjs`, `migrate-clips.mjs`; `git grep -ln schema --
'scripts/*.mjs'` — пусто при двух существующих схемах; `index.css` — 73 `@import`, 68
компонентов, 7 апстримных; `tokens.css:78-122` — склейка `ds-*` с селекторами компонентов,
включая `.ll-publish-btn` на 95; `avatar.css:79` — `__size-126 /* legacy */`; отсутствие
`schedule`/`cron` в `.github/workflows/` и `.sourcecraft/`; `new-vision/` и `koleso/` — 0
тегов `<base href>`, 0 подключений `proto-contain.js`; `new-vision/new-vision.css:17` и
пять `koleso/*.html:10` — прямые ссылки на `../index.css`; `preview.html` — продуктовые
компоненты в разметке на 701, 728, 759, 793, 891; `scripts/fetch-q3.mjs` — цели `FEEDS` на
строках 65 и 76 при одном `gid`; внешние хосты — 250 вхождений в атрибутах, 15 хостов;
`lottie-web` на 11 страницах, из них 7 через `s.src = '…'` (`lenta-q3.html:2577`); R9
целиком — 1344; Figma — 2 файла с полным `fileKey`, `inapp-push.css:11` без ключа, 29
уникальных id в 33 файлах при 44 вхождениях по методу «строка со словом figma»; структура
чек-листа `macket-rules-checker.md`; `git config core.hooksPath` вне репозитория → exit
128; `.dockerignore:13`; `data/around-you-media.json:155`; 209 открытых веток при 210
ссылках, merge-base у 21.

**Взято из предыдущей редакции без перепроверки** — эти числа сверял независимый
верификатор другим методом: 488 ссылок на `components/` (575 по репозиторию, 87 в `.md`);
2197 на `assets/`, из них 720 — `assets/icons/`, 692 внутри `FEED`-блоков; 17 на
`tokens.css`; 8 реальных тегов `<base href>`; 4 страницы с `proto-contain.js`; 82 CSS + 35
JS; 975 иконок; 509 объявлений при 427 именах; 46 страниц (47 `.html`, один партиал), 29 в
корне; 11 из 82 CSS с `Source:`; 121 файл под провенанс; 616 инлайн-`style=`, `<style>` на
45 из 46; расхождение форков 974/544/14/20; `--offline` у 4 из 9; Figma — 9 сопоставимых
имён, 2 расхождения, потолок ~2%.

**Где я разошёлся с проверяющими и почему.** Ревьюер называл 18 внешних хостов, 37
уникальных Figma-id в 36 файлах при 59 вхождениях и «5 страниц через `new-vision.css`».
Мои прогоны дают 15 хостов (по атрибутам `src=`/`href=`), 29 id в 33 файлах при 44
вхождениях (по строкам со словом `figma`) и 4 страницы через `new-vision.css`. Расхождение
— в методе: более широкий шаблон ловит упоминания в комментариях, `preconnect` и строках
JS, а также ложные срабатывания вроде id фотографии Unsplash. Поэтому у каждого такого
числа в документе указан метод, а вывод сформулирован так, чтобы от метода не зависеть.

**Ограничения.** Клон **shallow** (`.git/shallow`, 51 коммит, 2026-07-09…08-02) — поэтому
аргументы «из истории правок файла» не используются нигде, а содержимое 188 веток
непроверяемо. Раздел про Figma опирается на read-only вызовы MCP, выполненные в
предыдущей сессии другим агентом; я их не повторял.

**Осталось непроверенным:** права на репозиторий, лимиты Actions и доступность
`actions/github-script` (вопрос B); поведение workflow на PR из форков именно здесь;
`fileKey` для голых нод; что именно трогают 188 веток; заявление
`.sourcecraft/sites.yaml` о публикации корня — в YAML есть только `ref: "main"`, вывод про
корень взят из комментария в файле.

**Что снято из «непроверенного» по итогам ревью:** доля ложных срабатываний правила R1.
Она проверена и воспроизводима: шаблоны рендера в `scripts/fetch-*.mjs` имеют базой
целевую страницу, а не папку скрипта, поэтому правило ошибается в обе стороны на файлах
пайплайна. Отсюда область R1 сужена до `*.html` и `*.css` (Р5).

**Что в репозитории изменено этой работой:** создан и переписан только этот файл.

---

## Приложение А. Откуда это выросло

Первым заходом был разбор раздела «10. Сцепление и цена реорганизации» из
`docs/architecture.md` — там впервые посчитали, чем слои сцеплены, и предложили две
корзины `ds/` + `app/`. Разбор был построен как ответ на чужой текст и потому наследовал
его логику: решения оценивались стоимостью перехода, а «дорого» и «сломает ссылки»
работали как аргументы против. Настоящий документ переписан от целевого состояния; цена
перехода осталась там, где ей место, — в разделе 4, где она определяет порядок и способ,
а не в вердиктах.

## Приложение Б. Сводка замеров

Метод указан там, где число от него зависит.

| Что | Значение | Метод |
|---|---|---|
| HTML-страниц | 46 (47 `.html`, один — партиал), 29 в корне | `git ls-files '*.html'` |
| Точка входа стилей | 36 подключают `index.css` напрямую; 4 — через `new-vision/new-vision.css`; 6 без общей точки входа (5 содержательных + `index.html`) | `git grep -l` |
| `components/` | 82 CSS + 35 JS + 1 партиал | `git ls-files components/` |
| Файлов ДС под провенанс | 121 (82 + 35 + 4 корневых CSS); с `Source:` — 11 | `grep -l 'Source:'` |
| `index.css` | 73 `@import`, из них 68 компонентов; апстримных — 7 | чтение файла |
| `tokens.css` | 509 объявлений, 427 уникальных имён; склейка с компонентами — 78-122 | `grep -oE '^\s*--'` |
| Иконок | 975 | `git ls-files assets/icons` |
| Ссылок на `components/` | 488 в 109 файлах (575 по репозиторию, 87 в `.md`) | `git grep -o` по `html/css/js/mjs/json` |
| Ссылок на `assets/` | 2197 в 111 файлах; 720 — `assets/icons/`; 692 в `FEED`-блоках | там же |
| Ссылок на `tokens.css` | 17 | там же |
| Тегов `<base href="../">` | 8 | `git grep '^<base href'` |
| Страниц с `proto-contain.js` | 4 | `git grep -l` |
| Генераторов | 9 + оркестратор; `--offline` у 4; читают схему — 0 | `grep -ln` |
| Литералы цвета (R9) | 1344 | `git grep -oE '#[0-9a-fA-F]{3,8}\b\|rgba?\(\|hsla?\('` по `html/css/js/mjs` |
| Инлайн-`style=` / `<style>` | 616 / на 45 страницах из 46 | `git grep -o` |
| Внешние сети | 250 вхождений, 15 хостов | `git grep -oE '(src\|href)="https?://[a-z0-9.-]+' -- '*.html'` |
| — из них не видно этим замером | `lottie-web` на 11 страницах, атрибутом — на 4; остальные 7 через `s.src = '…'` | `git grep -n 'cdn.jsdelivr'` |
| Каналов публикации | 3 (GitHub Pages, SourceCraft Sites, VM) | чтение конфигов |
| Персональные данные | 38 имён в `data/people.json`, 38 фото в `assets/people` (4 МБ) | `ls`, `grep -c '"name"'` |
| Открытых веток | 209 (merge-base доступен у 21) | `git ls-remote --heads origin` |
| Ссылки на Figma-ноды | 2 файла с полным `fileKey`; 29 уникальных id в 33 файлах, 44 вхождения | `git grep -i figma` + id-шаблон |
| Что давала сверка с Figma | 9 сопоставимых имён из 427, 2 расхождения, потолок автосборки ~2%; режимы и слой `--base-*` не отдаются | MCP, предыдущая сессия |
