# Не тащим в образ служебное и тяжёлое — оно не нужно server.mjs в рантайме.
# .git ИСКЛЮЧАЕМ: история (webp) огромная, в образе переполняла диск VM при сборке.
# Контейнеру для коммита/пуша .git даём монтированием /opt/proto-moon (см. docker-compose).
.git
.github
.gitverse
.sourcecraft
.claude
node_modules
*.md
*.sh
.nojekyll
.fig2.png
Dockerfile
.dockerignore
