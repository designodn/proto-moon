{
  "name": "proto-moon",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Статический прототип OK DS: контент запекается локально скриптами scripts/fetch-*.mjs и коммитится в репозиторий",
  "scripts": {
    "start": "node server.mjs",
    "sync": "node scripts/fetch-all.mjs",
    "test": "node --test scripts/lib/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "sharp": "^0.35.2"
  }
}
